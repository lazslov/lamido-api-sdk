/*
 * Generated from contracts/webshop-service.openapi.yaml by `pnpm generate:types`.
 * Do not edit: CI regenerates this file and fails on any diff.
 *
 * These are SHAPES only. Behaviour — what a 404 means, when a retry is safe, what an
 * omitted field means — is hand-written from the knowledge base's Markdown and lives in
 * each package's curated type aliases. Where the two disagree the Markdown wins.
 */
/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/healthz": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Liveness probe
         * @description Unauthenticated and unversioned. NEVER answers 503: a degraded dependency is reported as `status: "degraded"` in the body with a 200, so a monitor that checks `response.ok` before reading the body still sees the reason. `configuration` is checked before the database, and `database` reads `"unknown"` (not `"degraded"`) when the environment is incomplete.
         */
        get: operations["getLiveness"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/public/products": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List published products (browser tier)
         * @description Only `published` products, with archived variants filtered out. Sets `ETag` and `Cache-Control: public, s-maxage=60, stale-while-revalidate=300`. Send `If-None-Match` for a 304. NOTE: the actual response carries no `Access-Control-Allow-Origin` header at this commit, so a cross-origin browser read is blocked - see public-api.md.
         */
        get: operations["listPublicProducts"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/public/products/{id_or_slug}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * One published product by public_id or slug (browser tier)
         * @description A 404 here is cached with `Cache-Control: public, s-maxage=10` and carries no ETag, so a newly published product may 404 for up to ten seconds.
         */
        get: operations["getPublicProduct"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/me": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * The calling shop and key
         * @description `shop.status` is deliberately absent - a suspended shop's keys all answer 401, so a shop that receives this response is active by definition.
         */
        get: operations["getStorefrontSelf"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/products": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List published products (secret tier)
         * @description Identical visibility and shape to the public tier, from the same implementation. No cache headers of any kind are set here.
         */
        get: operations["listStorefrontProducts"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/products/{id_or_slug}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** One published product by public_id or slug (secret tier) */
        get: operations["getStorefrontProduct"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/carts": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Create a cart
         * @description NOT idempotent: each call makes a new cart. `currency` is absent from the schema on purpose - a cart copies the shop's currency and may not choose. A cart is not scoped to a customer or a session; both fields are labels you attach for your own use.
         */
        post: operations["createCart"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/carts/{public_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read a cart, priced
         * @description An expired or converted cart still READS. Only writes are refused.
         */
        get: operations["getCart"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/carts/{public_id}/items": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Add a variant, or increase its existing line
         * @description NOT idempotent - it ADDS. Two calls of `quantity: 2` make 4. Adding a variant already in the cart increases that line, and the 99 cap applies to the resulting LINE, not the request. Another shop's `variant_id` answers 422 `variant_unavailable`, not 404, because the id arrived in the body.
         */
        post: operations["addCartItem"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/carts/{public_id}/items/{item_public_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * Remove a line
         * @description Answers 200 with the recalculated cart, not 204. An expired cart refuses this.
         */
        delete: operations["deleteCartItem"];
        options?: never;
        head?: never;
        /**
         * Set a line's quantity
         * @description Idempotent - `quantity` is absolute, not a delta.
         */
        patch: operations["setCartItemQuantity"];
        trace?: never;
    };
    "/v1/carts/{public_id}/coupon": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Apply or replace the cart's single coupon code
         * @description A cart holds at most one code, so this POST is safely repeatable. The coupon is judged now AND again at checkout. A refused coupon leaves any existing code untouched.
         */
        post: operations["applyCartCoupon"];
        /**
         * Remove the cart's coupon code
         * @description No validation of any kind. Removing a code that was never set is a safe no-op.
         */
        delete: operations["removeCartCoupon"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/carts/{public_id}/shipping-options": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * The shop's active shipping methods
         * @description IGNORES THE CART ENTIRELY - no weight, no destination, no value banding. It hangs off the cart so that carriage can depend on the cart later without moving the route. Only `active` methods are listed, and that is not a caller-controlled filter.
         */
        get: operations["listCartShippingOptions"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/carts/{public_id}/shipping-method": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        /**
         * Choose carriage, or clear the choice
         * @description The `shipping_method_id` key must be PRESENT; `null` clears the choice. A withdrawn method or another shop's answers 422 `shipping_method_inactive`.
         */
        put: operations["setCartShippingMethod"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/carts/{public_id}/checkout": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Turn the cart into an order
         * @description THE ONLY ENDPOINT THAT TAKES `Idempotency-Key`, AND IT IS REQUIRED. Everything happens in one transaction or nothing does. Every 400 and 422 RELEASES the key, so you may fix the request and retry with the same key; only a committed order replays. A replay returns the frozen bytes verbatim with `Idempotent-Replay: true`.
         *     Nothing about money is in the payload - totals come from the cart. The order is created `pending`, and a payment IS created at payment-service after the transaction commits: the 201 carries a `payment` block with a `gateway_url` to redirect the buyer to. The order does NOT become `paid` here - that happens asynchronously when payment-service reports success.
         *     CRITICAL: the payment call happens AFTER the commit, so a 429 or either 502 STILL LEFT A REAL `pending` ORDER HOLDING STOCK. The correct reaction is to retry with the SAME Idempotency-Key, which takes the resume path: the committed order is reloaded and only the payment is retried. Starting a new cart strands the order, because the old cart is already `converted` and a second checkout of it answers 422 `cart_converted`.
         */
        post: operations["checkoutCart"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orders": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List this shop's orders
         * @description Newest first. `from` is inclusive and `until` exclusive, and both test `created_at`. An unknown `status` value is a 400 here - unlike the shops and products admin listings.
         */
        get: operations["listStorefrontOrders"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orders/{public_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** One order */
        get: operations["getStorefrontOrder"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orders/{public_id}/cancel": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Cancel a pending or payment_failed order
         * @description The reservation is released in the SAME transaction as the status change. Calling it twice is SAFE and answers 200 - a no-op transition writes a timeline row and moves no counter. A `fulfilled` or `refunded` order answers 422 `order_terminal`.
         */
        post: operations["cancelStorefrontOrder"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/me": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * The calling admin key
         * @description No scope required - a key must always be able to introspect itself.
         */
        get: operations["getAdminSelf"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/health": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Deep health check
         * @description No scope required, and 200 even when degraded. ELEVEN counts, and ALL TEN QUERY-BACKED ONES ARE ABSENT ENTIRELY when the database is degraded - a monitor must treat absent as "unknown", not as zero, or it will report everything healthy during an outage.
         *     Every count drills into a listing through the SAME EXPORTED PREDICATE (WH-26), so a number here is a worklist rather than a hint. `oversold_orders` is the only one that means ACT.
         */
        get: operations["getAdminHealth"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/shops": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List shops
         * @description Scope `shops:read`. An UNRECOGNISED `status` value is SILENTLY IGNORED and returns an unfiltered list - unlike the orders listing, which answers 400.
         */
        get: operations["listShops"];
        put?: never;
        /**
         * Provision a shop
         * @description Scope `shops:write`. `currency` is set here and can NEVER be changed. `slug` is GLOBALLY unique. `tax_rate_bps: 27` is accepted and means 0.27% - read the echoed value.
         */
        post: operations["createShop"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/shops/{public_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * One shop
         * @description Scope `shops:read`.
         */
        get: operations["getShop"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /**
         * Update a shop, including suspending it
         * @description Scope `shops:write`. `currency` and `slug` are NOT patchable. Setting `status: "suspended"` takes the storefront offline immediately: every call with any of that shop's keys then answers 401, indistinguishably from a bad key.
         */
        patch: operations["updateShop"];
        trace?: never;
    };
    "/v1/admin/shops/{public_id}/keys": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * A shop's API keys
         * @description Scope `shops:read`. UN-PAGINATED - no `limit`, no `cursor` - and still returns the list envelope with `next_cursor: null`. Includes revoked keys. Never returns a plaintext key.
         */
        get: operations["listShopKeys"];
        put?: never;
        /**
         * Mint a shop key
         * @description Scope `shops:write`. THE `key` MEMBER IN THIS RESPONSE IS THE ONLY TIME THE PLAINTEXT IS EVER SHOWN. It is stored as a SHA-256 digest and cannot be recovered. No cap on keys per shop.
         */
        post: operations["mintShopKey"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/shops/{public_id}/keys/{key_id}/rotate": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Mint a replacement key and revoke this one
         * @description Scope `shops:write`. THE OLD KEY STOPS WORKING THE INSTANT THIS RETURNS - there is no overlap window. For a planned rotation, mint a second key alongside instead. Answers 201 with the NEW key and its plaintext.
         */
        post: operations["rotateShopKey"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/shops/{public_id}/keys/{key_id}/revoke": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Revoke a shop key
         * @description Scope `shops:write`. Idempotent - revoking twice answers 200 and moves nothing.
         */
        post: operations["revokeShopKey"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/shops/{public_id}/products": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Create a product with its variants
         * @description Scope `catalog:write`. Creation lives under the shop because it is the one catalog operation that cannot infer a shop from an existing row. OMITTING `variants` CREATES ONE DEFAULT VARIANT priced `"0"` with the product's own name - a product with no variant could never be added to a cart. Product and variants are one transaction. Born `draft`.
         */
        post: operations["createProduct"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/shops/{public_id}/coupons": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Create a coupon
         * @description Scope `discounts:write`. `value` is BASIS POINTS for `percentage` and MINOR UNITS for `fixed_amount`, always as a decimal string. `currency` is required for `fixed_amount` and must equal the shop's. There is no delete - withdraw with `active: false`.
         */
        post: operations["createCoupon"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/shops/{public_id}/shipping-methods": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Create a shipping method
         * @description Scope `orders:write`, NOT a `shipping:*` pair - the estate's scope map is shared and lives in lamido-admin. `price` is gross, like every price; `"0"` is free shipping. There is no delete - withdraw with `active: false`.
         */
        post: operations["createShippingMethod"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/admin-keys": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List admin keys
         * @description Scope `admin:manage`. Un-paginated, newest first, includes revoked keys. `?allowlist=empty` is the drill-down behind the `unrestricted_admin_keys` health count, through the same predicate; it also drops revoked keys, because the count is of LIVE keys. Any other query value is ignored and the unfiltered list is returned.
         */
        get: operations["listAdminKeys"];
        put?: never;
        /**
         * Mint an admin key
         * @description Scope `admin:manage`. OMITTING `scopes` GRANTS `["*"]` - the default is unrestricted. An unknown scope member fails the whole request. `allowed_ips` is exact string matching with NO CIDR SUPPORT; empty means unrestricted, and a non-empty list fails CLOSED when the caller IP cannot be determined. The plaintext appears exactly once.
         *     IN PRODUCTION AN EMPTY `allowed_ips` IS A 400 unless `allow_any_ip` is true. Outside production the old default stands. The opt-in is its own member because `allowed_ips: []` means "any address" and reads like "no address".
         */
        post: operations["mintAdminKey"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/admin-keys/{public_id}/revoke": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Revoke an admin key
         * @description Scope `admin:manage`. Idempotent. TWO GUARDS REFUSE THE CHANGE so the service cannot lock itself out: a key CANNOT REVOKE ITSELF (`key_self_revoke`), and the LAST LIVE KEY HOLDING `admin:manage` cannot be revoked (`last_managing_key`). Both are 422. Rotation is mint, move the consumer, then revoke the old key, and that order satisfies both guards.
         *     The bootstrap CLI enforces the second guard and not the first - it authenticates with the database URL, so there is no calling key. That asymmetry is the recovery path.
         */
        post: operations["revokeAdminKey"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/products": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List products across shops
         * @description Scope `catalog:read`. Cross-shop by default; narrow with `?shop=`. An UNRECOGNISED `status` value is SILENTLY IGNORED. Every product carries its variants including archived ones.
         */
        get: operations["listAdminProducts"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/products/{public_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * One product, with archived variants
         * @description Scope `catalog:read`.
         */
        get: operations["getAdminProduct"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /**
         * Update a product
         * @description Scope `catalog:write`. `status` is ABSENT from the schema on purpose - it moves only through the publish and archive verbs. `metadata` REPLACES the whole object; there is no merge.
         */
        patch: operations["updateProduct"];
        trace?: never;
    };
    "/v1/admin/products/{public_id}/publish": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Publish a product
         * @description Scope `catalog:write`. Legal from `draft` and `archived`. A no-op (already published) is a safe 200 that writes nothing at all - no status write, no audit row.
         */
        post: operations["publishProduct"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/products/{public_id}/archive": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Archive a product
         * @description Scope `catalog:write`. Legal from `draft` and `published`, and NOT a dead end - an archived product can be republished. Archiving makes every variant unbuyable IMMEDIATELY, including lines already sitting in open carts, which flip to `unavailable: true` and block checkout.
         */
        post: operations["archiveProduct"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/products/{public_id}/variants": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Add a variant to a product
         * @description Scope `catalog:write`. Answers 201 with the VARIANT alone, not the product. `currency` must equal the shop's; `sku` is unique per shop when present.
         */
        post: operations["createVariant"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/variants/{public_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * One variant
         * @description Scope `catalog:read`.
         */
        get: operations["getVariant"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /**
         * Update a variant, including its price
         * @description Scope `catalog:write`. `status` is not patchable, so a variant's archive is one-way. REPRICING CHANGES WHAT EVERY OPEN CART WILL BE CHARGED, because a cart reads the live price. It changes no existing order - order lines copied their price at checkout.
         */
        patch: operations["updateVariant"];
        trace?: never;
    };
    "/v1/admin/variants/{public_id}/archive": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Archive a variant
         * @description Scope `catalog:write`. One-way - there is no un-archive, because `status` is not on the variant patch schema. Nothing in the catalog is ever deleted.
         */
        post: operations["archiveVariant"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/variants/{public_id}/inventory": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * A variant's stock
         * @description Scope `inventory:read`. A variant with no inventory row answers `tracked: false` with every count null - NOT TRACKED, NOT SOLD OUT, and not a 404. Counts are decimal STRINGS because the columns are bigint. `available` is computed as `quantity - reserved_quantity`.
         */
        get: operations["getVariantInventory"];
        /**
         * Set a variant's stock
         * @description Scope `inventory:write`. SETS rather than adds, which is what makes it safe to repeat. The first PUT on an untracked variant creates the row and imposes a ceiling where there was none. `reserved_quantity` is NOT settable by anyone; setting `quantity` below it answers 422 `insufficient_stock` with the reserved count in the detail.
         */
        put: operations["setVariantInventory"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/orders": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List orders across shops
         * @description Scope `orders:read`. Cross-shop by default; narrow with `?shop=`. An unknown `status` IS a 400 here. Same serializer as the storefront tier - an operator and a buyer must not disagree about what was bought.
         */
        get: operations["listAdminOrders"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/orders/{public_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * One order, from any shop
         * @description Scope `orders:read`. No `?shop=` needed.
         */
        get: operations["getAdminOrder"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/orders/{public_id}/fulfill": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Mark an order fulfilled
         * @description Scope `orders:write`. Legal from `confirmed` ONLY - a `pending` order answers 422 `invalid_transition`. Fulfilment moves no stock: the reservation was committed when the payment succeeded.
         *     An order reaches `confirmed` only by consuming a `payment.succeeded` from payment-service, or by the daily reconcile poll. THERE IS NO ROUTE ON ANY TIER THAT MAKES AN ORDER `confirmed`, so if this refuses and you believe the order was paid, the fault is in the inbound path or the queue - not in a missing route. See workflows.md's runbook.
         */
        post: operations["fulfillOrder"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/orders/{public_id}/cancel": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Cancel an order as an operator
         * @description Scope `orders:write`. Legal from `pending` and `payment_failed`. The reservation is released in the SAME transaction. Cancelling twice answers 200.
         *     `inventory.release_expired` now reclaims lapsed holds too, but IT RUNS DAILY - so this remains the only way to reclaim stock from an abandoned checkout PROMPTLY.
         *     CANCELLING DOES NOT CANCEL THE PAYMENT: this service tells payment-service nothing. A buyer who cancels and then completes the gateway redirect produces an inbound `payment.succeeded` for a `canceled` order, which is recorded as a NO-OP because `canceled` is terminal. The money is at payment-service and a human resolves it there.
         */
        post: operations["cancelAdminOrder"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/coupons": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List coupons across shops
         * @description Scope `discounts:read`. An unrecognised `active` value is SILENTLY IGNORED. A coupon is never serialised to a buyer - there is no consumer-tier coupon endpoint.
         */
        get: operations["listCoupons"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/coupons/{public_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * One coupon
         * @description Scope `discounts:read`.
         */
        get: operations["getCoupon"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /**
         * Update a coupon
         * @description Scope `discounts:write`. The two cross-field rules are evaluated against the RESULTING coupon, not the patch, so changing only `type` to `fixed_amount` on a coupon with no currency is refused. `usage_count` is on no schema - it moves only through a redemption.
         */
        patch: operations["updateCoupon"];
        trace?: never;
    };
    "/v1/admin/shipping-methods": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List shipping methods across shops
         * @description Scope `orders:read`. An unrecognised `active` value is SILENTLY IGNORED.
         */
        get: operations["listShippingMethods"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/shipping-methods/{public_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * One shipping method
         * @description Scope `orders:read`.
         */
        get: operations["getShippingMethod"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /**
         * Update a shipping method
         * @description Scope `orders:write`. WITHDRAWING A METHOD A CART HAS CHOSEN MAKES THAT CHECKOUT FAIL: the cart quietly reads `shipping_method_id: null` and the checkout answers 422 `shipping_method_inactive`. Repricing changes what open carts will be charged.
         */
        patch: operations["updateShippingMethod"];
        trace?: never;
    };
    "/v1/admin/event-types": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * The event catalogue as data
         * @description WH-4. No shop, no filter, no pagination: it is the same for every caller. It exists so that a trigger checkbox list in an operator UI is not a hardcoded array in a sixth repository that must be edited whenever any of five services adds an event.
         */
        get: operations["listEventTypes"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/webhook-endpoints": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Every shop's webhook endpoints
         * @description Cross-shop by default. Scope `webhooks:read`. `?event_type=` answers "WHICH ENDPOINTS WOULD THIS EVENT REACH?" through the EMITTER'S OWN fan-out predicate, so the listing and the delivery cannot disagree - an endpoint with `subscribed_events: null` matches every type. It does NOT filter on `enabled`: a disabled endpoint still carries its subscription, and is usually what the operator is looking for. Combine with `?enabled=` for the fan-out set.
         */
        get: operations["listWebhookEndpoints"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/shops/{public_id}/webhook-endpoints": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * One shop's webhook endpoints
         * @description WH-9 route 3. Scope `webhooks:read`.
         */
        get: operations["listShopWebhookEndpoints"];
        put?: never;
        /**
         * Create a webhook endpoint
         * @description WH-9 route 4, and the ONLY route that returns the endpoint's signing secret. Creation lives under a shop because an endpoint belongs to a shop that cannot be inferred from an endpoint that does not exist yet - the same shape as a product's or a coupon's create.
         *     The URL is checked for deliverability at save time (DNS, and the SSRF guard) and CHECKED AGAIN at delivery, because a save proves only what was true at one instant. Scope `webhooks:write`.
         */
        post: operations["createWebhookEndpoint"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/webhook-endpoints/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * One webhook endpoint
         * @description Scope `webhooks:read`. No shop segment - the shop is read off the endpoint.
         */
        get: operations["getWebhookEndpoint"];
        put?: never;
        post?: never;
        /**
         * Delete a webhook endpoint
         * @description THE ONLY HARD DELETE IN THIS SERVICE. Everything else archives, deactivates or revokes. It cascades to this endpoint's deliveries; the `webhook_events` rows SURVIVE, because an event is a record of something that happened and deleting a destination does not un-happen it. Scope `webhooks:write`.
         */
        delete: operations["deleteWebhookEndpoint"];
        options?: never;
        head?: never;
        /**
         * Update a webhook endpoint
         * @description Scope `webhooks:write`. A changed `url` is re-checked for deliverability.
         *     Toggling `include_customer` writes a SECOND, DEDICATED audit row (`webhook_endpoint.set_include_customer`) on both enable and disable, as well as the generic update row - because a generic row naming `include_customer` among five changed fields is not an answer to "when did this endpoint start receiving customer addresses".
         */
        patch: operations["updateWebhookEndpoint"];
        trace?: never;
    };
    "/v1/admin/webhook-endpoints/{id}/rotate-secret": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Rotate an endpoint's signing secret
         * @description Mints a new `whsec_` and returns it ONCE. Scope `webhooks:write`.
         *     CRITICAL: the old secret stops working immediately. Update the receiver in the same window or every delivery fails its signature check at the receiver's end - which looks to them like an attack rather than a rotation.
         */
        post: operations["rotateWebhookEndpointSecret"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/webhook-endpoints/{id}/enable": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Enable an endpoint
         * @description Clears `disabled_reason` AND resets `consecutive_failures` to zero. Both, in one call: an endpoint re-enabled with its counter still one below the threshold would auto-disable again on the next single failure. Scope `webhooks:write`.
         */
        post: operations["enableWebhookEndpoint"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/webhook-endpoints/{id}/disable": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Disable an endpoint
         * @description Nothing more is delivered until it is re-enabled. Event rows are still WRITTEN - "did anything happen at 12:46?" must not depend on whether there was somewhere to send it. Scope `webhooks:write`.
         */
        post: operations["disableWebhookEndpoint"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/webhook-endpoints/{id}/test": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Send a webhook.ping
         * @description Delivers the reserved `webhook.ping` SYNCHRONOUSLY, so the response tells you what the receiver said. `webhook.ping` is in no catalogue and is not subscribable; it writes NO event row and NO delivery row, and it BYPASSES THE LADDER entirely - so a failure here is a failure now, not something that will be retried. Scope `webhooks:write`.
         */
        post: operations["testWebhookEndpoint"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/webhook-events": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * The emitted events, newest first
         * @description Immutable rows, written inside the transaction that made each fact true. `data.customer` is STRIPPED on the way out of this listing regardless of any endpoint's `include_customer`. `from` is inclusive and `until` exclusive, both against `occurred_at` - NOT `created_at`, unlike every other listing in this spec. Scope `webhooks:read`.
         */
        get: operations["listWebhookEvents"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/webhook-deliveries": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Delivery attempts and their outcomes
         * @description One row per (event, endpoint) pair - NEVER two, because a redelivery RESETS the row rather than inserting another. `?status=pending` and `?status=dead_lettered` use the same predicates as the two health counts, and `?status=dead_lettered&cross_service=true` COMPOSES exactly the `cross_service_dead_letters` predicate out of its two halves. Scope `webhooks:read`.
         */
        get: operations["listWebhookDeliveries"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/webhook-deliveries/{id}/redeliver": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Resend a delivery
         * @description Resets the delivery row to `pending` with `attempt: 0` and BURSTS THE QUEUE INLINE after the response - so an operator sees something happen rather than waiting for the next daily cron tick. This is one of the three drain triggers.
         *     The response returns as soon as the job is queued: `redelivering: true` means "started", not "delivered". Poll the delivery listing for the outcome. Scope `webhooks:write`.
         */
        post: operations["redeliverWebhookDelivery"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/jobs": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * The job queue
         * @description Every kind of background work in one table. Scope `admin:manage` - see the warning above.
         */
        get: operations["listJobs"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/jobs/{id}/retry": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Requeue a job
         * @description `attempts` RESETS TO ZERO. An operator retrying is asserting that whatever killed the job is fixed; leaving the counter one rung below the cap would send it straight back to `dead` on the next failure, which reads as "the retry button does not work".
         *     It requeues; IT DOES NOT RUN THE JOB. The next drain does, and the scheduled drain is DAILY. To make it happen now, POST `/api/cron/webhooks-drain` - or, for a webhook specifically, use `.../redeliver`, which bursts inline. Scope `admin:manage`.
         */
        post: operations["retryJob"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/jobs/{id}/cancel": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Cancel a job
         * @description Sets `canceled_at`. Cancellation is a COLUMN, not a delete, so the trail survives. A canceled job is invisible to the drain and is excluded from the `dead_jobs` count. Scope `admin:manage`.
         */
        post: operations["cancelJob"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/inbound-events": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * The inbound event ledger
         * @description What arrived from payment-service and what was decided about it. READ-ONLY AND CROSS-SHOP: an inbound event may have resolved to no shop at all, which is exactly what `unknown_account` is for, so a shop-scoped listing could never show those.
         *     THE LEDGER HOLDS NO PAYLOAD, on purpose. It records what a delivery was and what we did, never its body: a refused signature must not leave unverified bytes in a table an operator browses, and an accepted event is already described by the order it moved.
         *     A BAD SIGNATURE WRITES NO ROW AT ALL - the ledger is keyed on `(source_service, event_id)` and an unverified body may carry no readable `event_id` to key on. So a signature attack is visible in the logs, not here. Scope `webhooks:read`.
         */
        get: operations["listInboundEvents"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/audit-log": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Who did what, newest first
         * @description The trail every mutation has written since migration `0000`, readable from phase 7. Scope `audit:read` AND NOTHING ELSE - reading who did what is a different privilege from doing it, so an auditor's key opens this and changes nothing.
         *     FILTERABLE BY `actor_type`, which HR-19 requires: a report that spans the admin and the consumer tier must not be silently partial. The filter is a CLOSED ENUM and an unknown value is a 400 rather than an empty page, because an empty page reads as "nothing happened".
         *     `target_id` WITHOUT `target_type` IS A 400. Ids are unique per resource kind and nothing else, so the pair is what identifies a target - and the unpaired query would scan without an index and return another kind's history.
         *     A MALFORMED TIMESTAMP IS A 400, never an ignored filter: a window silently widened to "everything" is the one failure an audit query must not have.
         *     `metadata` records WHICH KEYS CHANGED and never their values, allowlisted per action on the way in. There is no amount, no address and no email on any row here.
         *     READ-ONLY AT EVERY VERB. The table is append-only; a trail an operator can edit is not a trail.
         */
        get: operations["listAuditLog"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/shops/{public_id}/payment-credential": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * The credential readback
         * @description `last4` plus a fingerprint, and that is the WHOLE readback. There is no route that reveals the plaintext and the absence is the design rather than an omission. Scope `shops:read`.
         */
        get: operations["getPaymentCredential"];
        /**
         * Set or replace a shop's payment-service credential
         * @description One credential per shop. PUT rather than POST, and 200 rather than 201 EVEN THE FIRST TIME: the resource is identified by the shop, so a second save replaces the same thing and answering 201 twice would be a lie about what happened.
         *     The `pmk_` prefix is VALIDATED here rather than assumed, because the failure it prevents is silent: a key pasted without it authenticates against nothing at payment-service, and the first symptom would be every checkout in that shop answering 502 hours later.
         *     Replacing a credential RE-DRIVES NOTHING by itself; it fixes the next checkout. Scope `shops:write` - a `payments:*` pair would fit better and needs a scope-map change this repository does not own.
         */
        put: operations["putPaymentCredential"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/hooks/payment-service": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Receive an event from payment-service
         * @description NOT A SURFACE ANYONE ELSE CALLS, and it carries NO BEARER KEY of ours: payment-service authenticates with an HMAC `X-Signature` over the RAW request bytes. Requiring one of our keys as well would mean provisioning a credential in the direction the signature already covers.
         *     EVERYTHING EXCEPT A BAD SIGNATURE ANSWERS 200, including events we decide to ignore. A 4xx to an emitter means "retry this", and retrying an event we have decided to ignore produces eight identical deliveries we ignore eight times. So a 200 means RECEIVED, not ACTED ON - read `outcome` in the body.
         *     The receiver does no domain work: it verifies, resolves the shop from `account_id` ALONE, decides, records a ledger row and ENQUEUES. What a `payment.succeeded` means to an order is decided by a job, off the response path, so a slow re-reservation cannot time out at payment-service and become their retry storm.
         *     Freshness is deliberately NOT enforced on the signature timestamp: an emitter's ladder can legitimately deliver hours late on a sparse cron tier, and refusing it then would drop a real payment. Replay is stopped by the ledger's unique index instead, whatever the clock says.
         */
        post: operations["receivePaymentServiceEvent"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cron/webhooks-drain": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Drain the job queue
         * @description Claims a batch of up to 50 due jobs and runs them inside a 45-second wall budget, stopping BETWEEN jobs. It drains EVERY job type, not only deliveries: one queue, one claim, one loop.
         *     ANSWERS 200 WITH A REPORT EVEN WHEN IT DID NOTHING. Falling behind has to be visible as a rising `due_jobs` on admin health, never as a timeout.
         *     DAILY on this deployment's cron tier, which is why every retry rung in `webhooks.md` is a FLOOR rather than a promise. What makes an `order.created` arrive in milliseconds is the checkout's own inline post-commit burst, not this.
         */
        get: operations["cronWebhooksDrain"];
        put?: never;
        /**
         * Drain the job queue, driven by an operator
         * @description The same tick as the GET, for an operator holding the secret. Takes no body.
         */
        post: operations["cronWebhooksDrainByHand"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cron/sweeps": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Enqueue the retention and reconciliation sweeps, then drain
         * @description Enqueues four jobs — lapsed reservation, expired cart, expired replay record and the payment reconciliation poll — and then drains. It ENQUEUES RATHER THAN DOING THE WORK, so the sweeps ride the same claim lease, the same ladder and the same admin surface as every other job.
         *     Each sweep holds ONE LIVE JOB at a time under a unique key, so a tick that overlaps a slow previous tick enqueues nothing instead of running the same sweep twice. `enqueued` therefore lists what this tick actually added, which can be fewer than four.
         *     `payment.reconcile` rides this tick rather than a third cron, because the deployment's tier allows two. Its 30-minute age predicate is therefore a FLOOR: an order whose deliveries all dead-lettered waits for the next morning.
         */
        get: operations["cronSweeps"];
        put?: never;
        /**
         * Enqueue the sweeps, driven by an operator
         * @description The same tick as the GET, for an operator holding the secret. Takes no body.
         */
        post: operations["cronSweepsByHand"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /**
         * @description A decimal string of MINOR units. Never a JSON number. NEVER NEGATIVE - no amount in this service is, and a discount is a discount rather than a negative price. No leading zero and no sign: the pattern is the validator's own source. HUF, JPY, KRW, ISK, CLP and VND have ZERO minor units, so `"1000"` HUF is 1000 Ft. Everything else is two-decimal.
         * @example 4990
         */
        MinorAmount: string;
        /**
         * @description A non-negative whole number as a decimal STRING, because the column is bigint. A JSON number here is a 400.
         * @example 40
         */
        Count: string;
        /**
         * @description ISO 4217, upper case. One currency per shop, and every amount carries it.
         * @example HUF
         */
        Currency: string;
        /** @description Lower case, digits and underscore. No dashes, no upper case. */
        Slug: string;
        /** @description Opaque to this service and stored untouched. The one place unknown keys are ALLOWED; nesting is free. A PATCH REPLACES the whole object. */
        Metadata: {
            [key: string]: unknown;
        };
        ProblemFieldError: {
            /** @description An RFC 6901 JSON Pointer into the body (`/quantity`, `/variants/0/sku`), or `#/query/<name>`, or `#/header/Idempotency-Key`. */
            pointer: string;
            /** @enum {string} */
            code: "required" | "invalid_type" | "invalid_format" | "invalid_value" | "too_small" | "too_big" | "unknown_field" | "duplicate" | "currency_mismatch" | "out_of_range" | "invalid";
            detail: string;
        };
        /** @description RFC 9457 problem details. Every failure on every surface uses this shape. */
        Problem: {
            /**
             * @description Closed set. NOTE `conflict` covers BOTH 409 and 422, and `internal` covers BOTH 500 AND 502 - branch on `status` and `code`/`provider_error`, never on `type` alone. There is deliberately no `bad-gateway` member: the set is closed and adding one is an API change, so a 502 reuses `internal` and is distinguished by its status and `provider_error`. `payload-too-large` is unreachable at this commit.
             * @enum {string}
             */
            type: "urn:webshop-service:problem:validation" | "urn:webshop-service:problem:unauthorized" | "urn:webshop-service:problem:forbidden" | "urn:webshop-service:problem:not-found" | "urn:webshop-service:problem:conflict" | "urn:webshop-service:problem:payload-too-large" | "urn:webshop-service:problem:rate-limit" | "urn:webshop-service:problem:internal";
            /** @description Describes the HTTP STATUS, not the type. A 422 reads "Unprocessable Entity". */
            title: string;
            status: number;
            /** @description Prose for a human. NEVER branch on it. */
            detail: string;
            /** @description The request PATH only - never a full URL, never a query string. */
            instance: string;
            request_id: string | null;
            errors?: components["schemas"]["ProblemFieldError"][];
            /**
             * @description The branchable sub-reason, present on some 409 and 422 responses. `key_revoked` is in the enum and is NEVER RAISED - a revoked key is an ordinary 401. `key_self_revoke` and `last_managing_key` are both 422 on the revoke route: they refuse the CHANGE, so the service cannot lock itself out of its own admin tier. `endpoint_limit_reached` is a 409 on the admin tier: the shop already holds 20 webhook endpoints, and it is refused by a serialized COUNT rather than by a constraint, because a row count is not expressible as one.
             * @enum {string}
             */
            code?: "key_revoked" | "invalid_transition" | "variant_unavailable" | "insufficient_stock" | "cart_expired" | "cart_converted" | "order_terminal" | "coupon_invalid" | "coupon_expired" | "coupon_exhausted" | "coupon_minimum_not_met" | "shipping_method_inactive" | "idempotency_key_reused" | "idempotency_in_flight" | "endpoint_limit_reached" | "key_self_revoke" | "last_managing_key";
            /** @description Seconds. 429 only. */
            retry_after?: number;
            /**
             * @description 502 only, and the checkout's payment step is the only place either can arise. NO `code` accompanies these. `payment_create_rejected` means payment-service answered a definitive 4xx and the identical request cannot succeed - do not retry until the shop's configuration changes. `payment_create_unknown` means a timeout, a 5xx, a 408, a 429, or a 2xx whose body was not a payment: A PAYMENT MAY OR MAY NOT EXIST, so retry the SAME Idempotency-Key - the derived key guarantees one payment rather than two. Both leave a real `pending` order holding stock.
             * @enum {string}
             */
            provider_error?: "payment_create_rejected" | "payment_create_unknown";
        };
        Liveness: {
            /** @enum {string} */
            status: "ok" | "degraded";
            /** @enum {string} */
            configuration: "ok" | "invalid";
            /**
             * @description `"unknown"` when the configuration is invalid - nothing was measured, and claiming otherwise pointed a past incident at the wrong system.
             * @enum {string}
             */
            database: "ok" | "degraded" | "unknown";
            version: string;
            /** @description The deployed git SHA, or `"dev"` locally. This answers "which build is live?". */
            commit: string;
            /** Format: date-time */
            now: string;
        };
        AdminHealth: {
            /** @enum {string} */
            status: "ok" | "degraded";
            /** @enum {string} */
            database: "ok" | "degraded";
            /** @description Active shops with a null `external_ref`. Drills into `GET /v1/admin/shops?without_external_ref=true`. OVERDUE: the inbound receiver resolves a shop from `account_id` alone, so until this is zero every inbound event for an unbackfilled shop is recorded `unknown_account` and acted on by nothing. */
            shops_without_external_ref?: number;
            /** @description Drills into `GET /v1/admin/webhook-deliveries?status=pending`. */
            pending_deliveries?: number;
            /** @description Drills into `GET /v1/admin/webhook-deliveries?status=dead_lettered`. */
            dead_lettered_deliveries?: number;
            /** @description Drills into `GET /v1/admin/webhook-endpoints?enabled=false`. Above zero means an operator disabled one, or five consecutive exhausted ladders auto-disabled it. */
            disabled_endpoints?: number;
            /** @description Jobs eligible for the next drain. Drills into `GET /v1/admin/jobs?status=due`. Alert on a RISING TREND, not a threshold: the scheduled drain is daily, so this legitimately spikes between ticks. */
            due_jobs?: number;
            /** @description Jobs that spent their ladder, EXCLUDING canceled ones. Drills into `GET /v1/admin/jobs?status=dead`. */
            dead_jobs?: number;
            /** @description Drills into `GET /v1/admin/inbound-events?reason_code=unknown_account`. This is `shops_without_external_ref` observed from the receiving side. */
            unroutable_inbound_events?: number;
            /** @description Drills into `GET /v1/admin/inbound-events?outcome=refused`. Today this can only mean the hop limit was reached - i.e. a cycle. A bad signature writes NO row at all. */
            refused_inbound_events?: number;
            /** @description Shops that cannot take money; their checkouts return `payment: null`. Drills into `GET /v1/admin/shops?without_payment_credential=true`. */
            shops_without_payment_credential?: number;
            /** @description THE CROSS-SERVICE SUBSET of `dead_lettered_deliveries`, counted separately because the two need different people: a shop's own endpoint failing is that shop's problem and there are many shops, while an ESTATE RECEIVER failing is ours and there is one of it. Mixed into one number the second is invisible behind the first. A receiver that is refusing looks identical from here to one quietly doing nothing, and this number is the only signal that separates them - so ONE IS ALREADY A STORY. Drills into `GET /v1/admin/webhook-deliveries?status=dead_lettered&cross_service=true`. */
            cross_service_dead_letters?: number;
            /** @description THE ONE COUNT HERE THAT MEANS ACT, NOT WATCH. Confirmed orders whose stock was gone when payment landed: the reservation lapsed, the payment-success action could not re-reserve, and it CONFIRMED ANYWAY because money had been taken. Nothing resolves these automatically. Drills into `GET /v1/admin/orders?oversold=true`. */
            oversold_orders?: number;
            /** @description Live admin keys whose `allowed_ips` is empty, which means unrestricted. A WATCH count: unrestricted is a legitimate posture on a platform whose egress addresses rotate. What it must not be is one nobody decided, which is why minting in production now requires either `allowed_ips` or `allow_any_ip`. Drills into `GET /v1/admin/admin-keys?allowlist=empty`. */
            unrestricted_admin_keys?: number;
        };
        /** @description Every collection uses this. `data` is `[]`, never null. `next_cursor` is always present and null on the last or only page. There is no `total` anywhere. */
        ListEnvelope: {
            data: unknown[];
            next_cursor: string | null;
        };
        ShopSelf: {
            /** Format: uuid */
            public_id: string;
            slug: components["schemas"]["Slug"];
            name: string;
            currency: components["schemas"]["Currency"];
            /** @description BCP 47. */
            locale: string;
        };
        ShopKey: {
            /** Format: uuid */
            public_id: string;
            /** @enum {string} */
            kind: "publishable" | "secret";
            label: string;
            secret_last4: string;
            /** @description The first 8 hex characters of the stored SHA-256 digest. */
            fingerprint: string;
            /**
             * Format: date-time
             * @description Refreshed at most every 5 minutes. Not a liveness signal.
             */
            last_used_at: string | null;
            /** Format: date-time */
            revoked_at: string | null;
            /** Format: date-time */
            created_at: string;
        };
        ShopKeyMint: {
            /**
             * @description `publishable` mints a `wpk_` key; `secret` mints a `wsk_` key.
             * @enum {string}
             */
            kind: "publishable" | "secret";
            /** @description Becomes the audit identity. Make it say who holds the key. */
            label: string;
        };
        MintedShopKey: components["schemas"]["ShopKey"] & {
            /** @description THE PLAINTEXT, RETURNED HERE AND NOWHERE ELSE, EVER. Stored as a SHA-256 digest and not recoverable. A lost key is rotated, not recovered. */
            key: string;
        };
        ShopKeyList: components["schemas"]["ListEnvelope"] & {
            data?: components["schemas"]["ShopKey"][];
        };
        StorefrontSelf: {
            shop: components["schemas"]["ShopSelf"];
            key: components["schemas"]["ShopKey"];
        };
        Shop: {
            /** Format: uuid */
            public_id: string;
            slug: components["schemas"]["Slug"];
            name: string;
            currency: components["schemas"]["Currency"];
            locale: string;
            /** @description Basis points as a JSON NUMBER - `2700` is 27%. It is a rate, not an amount. `0` reports no tax. `27` is accepted and means 0.27%. */
            tax_rate_bps: number;
            /**
             * @description `suspended` makes every one of this shop's keys answer 401.
             * @enum {string}
             */
            status: "active" | "suspended";
            /**
             * Format: uuid
             * @description The lamido-admin account id. Written by lamido-admin, not by this service.
             */
            external_ref: string | null;
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            updated_at: string;
        };
        ShopCreate: {
            slug: components["schemas"]["Slug"];
            name: string;
            currency: components["schemas"]["Currency"];
            /** @default hu-HU */
            locale: string;
            /** @default 0 */
            tax_rate_bps: number;
            /**
             * Format: uuid
             * @default null
             */
            external_ref: string | null;
        };
        /** @description `currency` and `slug` are absent on purpose. Changing the currency would reinterpret every stored minor-unit amount. Sending `{}` is a valid no-op. */
        ShopUpdate: {
            name?: string;
            locale?: string;
            tax_rate_bps?: number;
            /** Format: uuid */
            external_ref?: string | null;
            /** @enum {string} */
            status?: "active" | "suspended";
        };
        ShopList: components["schemas"]["ListEnvelope"] & {
            data?: components["schemas"]["Shop"][];
        };
        AdminKey: {
            /** Format: uuid */
            public_id: string;
            label: string;
            secret_last4: string;
            fingerprint: string;
            scopes: components["schemas"]["AdminScope"][];
            /** @description Empty means UNRESTRICTED. Exact string matching - no CIDR support. */
            allowed_ips: string[];
            /** Format: date-time */
            last_used_at: string | null;
            /** Format: date-time */
            revoked_at: string | null;
            /** Format: date-time */
            created_at: string;
        };
        /**
         * @description Checked per route. `stats:read` and `audit:read` are in the enum and NO ROUTE CONSUMES THEM.
         *     THREE PAIRS ARE NEW AND EACH SITS BESIDE THE WIDER SCOPE IT SPLIT FROM, which still works: `jobs:*` beside `admin:manage` on the queue routes, `shipping:*` beside `orders:*` on the shipping methods, `payments:*` beside `shops:*` on the per-shop payment credential. The wide alternative is a COMPATIBILITY GRANT, not a design - no key can hold a new scope until lamido-admin's scope map carries it. It goes away when the map has shipped and the keys are reissued. Provision a queue monitor with `jobs:read` and nothing else: `admin:manage` also MINTS AND REVOKES ADMIN KEYS.
         * @enum {string}
         */
        AdminScope: "*" | "shops:read" | "shops:write" | "jobs:read" | "jobs:write" | "payments:read" | "payments:write" | "shipping:read" | "shipping:write" | "catalog:read" | "catalog:write" | "orders:read" | "orders:write" | "discounts:read" | "discounts:write" | "inventory:read" | "inventory:write" | "webhooks:read" | "webhooks:write" | "stats:read" | "audit:read" | "admin:manage";
        MintedAdminKey: components["schemas"]["AdminKey"] & {
            /** @description The plaintext, returned here and nowhere else, ever. */
            key: string;
        };
        AdminKeyMint: {
            /** @description Becomes the audit identity. Make it say who holds the key. */
            label: string;
            /** @description OMITTING THIS GRANTS `["*"]`. An unknown member fails the whole request. */
            scopes?: components["schemas"]["AdminScope"][];
            /** @default [] */
            allowed_ips: string[];
            /** @description The explicit "any address" opt-in. REQUIRED IN PRODUCTION when `allowed_ips` is empty. Cannot be combined with a non-empty `allowed_ips` - that pair is a 400. */
            allow_any_ip?: boolean;
        };
        AdminKeyList: components["schemas"]["ListEnvelope"] & {
            data?: components["schemas"]["AdminKey"][];
        };
        PublicVariant: {
            /**
             * Format: uuid
             * @description ADD THIS TO A CART, not the product id.
             */
            public_id: string;
            name: string;
            sku: string | null;
            price: components["schemas"]["MinorAmount"];
            /** @description The struck-through price. NOT validated to exceed `price`, but it is an amount like any other and a negative one is a 400. */
            compare_at_price: components["schemas"]["MinorAmount"] | null;
            currency: components["schemas"]["Currency"];
        };
        /** @description The storefront shape, served identically to the `wpk_` and `wsk_` tiers. `status` and `metadata` are absent, and archived variants are filtered out. */
        PublicProduct: {
            /** Format: uuid */
            public_id: string;
            slug: components["schemas"]["Slug"];
            name: string;
            description: string | null;
            product_type: components["schemas"]["ProductType"];
            /** @description Never empty. Ordered by `(position, public_id)` ascending. */
            variants: components["schemas"]["PublicVariant"][];
            /**
             * Format: date-time
             * @description The PRODUCT row only. A price change moves a VARIANT row.
             */
            updated_at: string;
        };
        PublicProductList: components["schemas"]["ListEnvelope"] & {
            data?: components["schemas"]["PublicProduct"][];
        };
        /**
         * @description Modelled and NOTHING BRANCHES ON IT. Informational only.
         * @enum {string}
         */
        ProductType: "physical" | "digital" | "service";
        AdminVariant: {
            /** Format: uuid */
            public_id: string;
            name: string;
            sku: string | null;
            price: components["schemas"]["MinorAmount"];
            compare_at_price: string | null;
            currency: components["schemas"]["Currency"];
            position: number;
            /** @enum {string} */
            status: "active" | "archived";
            metadata: components["schemas"]["Metadata"];
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            updated_at: string;
        };
        AdminProduct: {
            /** Format: uuid */
            public_id: string;
            slug: components["schemas"]["Slug"];
            name: string;
            description: string | null;
            product_type: components["schemas"]["ProductType"];
            /** @enum {string} */
            status: "draft" | "published" | "archived";
            metadata: components["schemas"]["Metadata"];
            /** @description Includes ARCHIVED variants, unlike the storefront shape. */
            variants: components["schemas"]["AdminVariant"][];
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            updated_at: string;
        };
        AdminProductList: components["schemas"]["ListEnvelope"] & {
            data?: components["schemas"]["AdminProduct"][];
        };
        VariantCreate: {
            name: string;
            price: components["schemas"]["MinorAmount"];
            compare_at_price?: string | null;
            currency: components["schemas"]["Currency"];
            sku?: string | null;
            position?: number;
            metadata?: components["schemas"]["Metadata"];
        };
        /** @description `status` is absent, so a variant's archive is one-way. */
        VariantUpdate: {
            name?: string;
            price?: components["schemas"]["MinorAmount"];
            compare_at_price?: string | null;
            currency?: components["schemas"]["Currency"];
            sku?: string | null;
            position?: number;
            metadata?: components["schemas"]["Metadata"];
        };
        ProductCreate: {
            slug: components["schemas"]["Slug"];
            name: string;
            description?: string | null;
            product_type?: components["schemas"]["ProductType"];
            metadata?: components["schemas"]["Metadata"];
            /** @description OMITTING THIS creates ONE default variant with the product's own name, price `"0"` and the shop's currency. */
            variants?: components["schemas"]["VariantCreate"][];
        };
        /** @description `status` is absent - it moves only through the publish and archive verbs. */
        ProductUpdate: {
            slug?: components["schemas"]["Slug"];
            name?: string;
            description?: string | null;
            product_type?: components["schemas"]["ProductType"];
            metadata?: components["schemas"]["Metadata"];
        };
        /** @description `tracked: false` with every count null means NOT TRACKED - unlimited, not sold out. Inventory is opt-in per variant. */
        Inventory: {
            /** Format: uuid */
            variant_public_id: string;
            tracked: boolean;
            /** @description A decimal string count, or null when untracked. */
            quantity: string | null;
            /** @description Held by pending orders. NOT SETTABLE through any API. */
            reserved_quantity: string | null;
            /** @description Computed as `quantity - reserved_quantity`. Stored nowhere. */
            available: string | null;
            /** Format: date-time */
            updated_at: string | null;
        };
        InventorySet: {
            quantity: components["schemas"]["Count"];
        };
        /** @description The buyer's view - a strict subset of the operator's. */
        ShippingOption: {
            /** Format: uuid */
            public_id: string;
            name: string;
            description: string | null;
            price: components["schemas"]["MinorAmount"];
            currency: components["schemas"]["Currency"];
        };
        ShippingOptionList: components["schemas"]["ListEnvelope"] & {
            data?: components["schemas"]["ShippingOption"][];
        };
        ShippingMethod: components["schemas"]["ShippingOption"] & {
            /** @description There is no delete. Withdraw with `active: false`. */
            active: boolean;
            metadata: components["schemas"]["Metadata"];
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            updated_at: string;
        };
        ShippingMethodCreate: {
            name: string;
            description?: string | null;
            price: components["schemas"]["MinorAmount"];
            currency: components["schemas"]["Currency"];
            /** @default true */
            active: boolean;
            metadata?: components["schemas"]["Metadata"];
        };
        ShippingMethodUpdate: {
            name?: string;
            description?: string | null;
            price?: components["schemas"]["MinorAmount"];
            currency?: components["schemas"]["Currency"];
            active?: boolean;
            metadata?: components["schemas"]["Metadata"];
        };
        ShippingMethodList: components["schemas"]["ListEnvelope"] & {
            data?: components["schemas"]["ShippingMethod"][];
        };
        /** @enum {string} */
        CouponType: "percentage" | "fixed_amount";
        /** @description NEVER serialised to a buyer. There is no consumer-tier coupon endpoint. */
        Coupon: {
            /** Format: uuid */
            public_id: string;
            /** @description Stored UPPER CASE, unique per shop. */
            code: string;
            type: components["schemas"]["CouponType"];
            /** @description One field, two meanings: BASIS POINTS for `percentage` (`"1000"` is 10%, max `"10000"`), MINOR UNITS for `fixed_amount`. A decimal string either way. */
            value: string;
            /** @description Required for `fixed_amount`, meaningless for `percentage`. */
            currency: string | null;
            /** @description Measured against the SUBTOTAL, before the discount and before carriage. */
            minimum_order_value: string | null;
            /** @description A JSON number. Null is unlimited. */
            usage_limit: number | null;
            /** @description Read only. It moves only through a redemption. */
            usage_count: number;
            /**
             * Format: date-time
             * @description Inclusive.
             */
            starts_at: string | null;
            /**
             * Format: date-time
             * @description Exclusive.
             */
            expires_at: string | null;
            active: boolean;
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            updated_at: string;
        };
        CouponCreate: {
            /** @description Case-insensitive on the way in; stored upper case. */
            code: string;
            type: components["schemas"]["CouponType"];
            /** @description Above zero, no leading zero. Basis points or minor units, per `type`. */
            value: string;
            /** @description REQUIRED for `fixed_amount`, and must equal the shop's currency. */
            currency?: string | null;
            minimum_order_value?: string | null;
            usage_limit?: number | null;
            /** Format: date-time */
            starts_at?: string | null;
            /** Format: date-time */
            expires_at?: string | null;
            /** @default true */
            active: boolean;
        };
        /** @description The cross-field rules are checked against the RESULTING coupon, not the patch. `usage_count` is deliberately absent. */
        CouponUpdate: {
            code?: string;
            type?: components["schemas"]["CouponType"];
            value?: string;
            currency?: string | null;
            minimum_order_value?: string | null;
            usage_limit?: number | null;
            /** Format: date-time */
            starts_at?: string | null;
            /** Format: date-time */
            expires_at?: string | null;
            active?: boolean;
        };
        CouponList: components["schemas"]["ListEnvelope"] & {
            data?: components["schemas"]["Coupon"][];
        };
        /** @description A line names its PRODUCT, not only its variant. `"Lifetime access"` alone does not say what a buyer is about to pay for, so a storefront given the variant name alone has to map `variant_public_id` back through the catalog to render its own cart. */
        CartLine: {
            /**
             * Format: uuid
             * @description The id used in the PATCH and DELETE item paths.
             */
            public_id: string;
            /** Format: uuid */
            variant_public_id: string;
            /**
             * Format: uuid
             * @description The product this variant belongs to - for a link back to your product page.
             */
            product_public_id: string;
            /** @description The natural key your own product route is built on (HR-9). */
            product_slug: string;
            /** @description WHAT THE BUYER THINKS THEY ARE BUYING. */
            product_name: string;
            /** @description The VARIANT's own name, e.g. `"1 kg whole bean"`. It was `name` until a product name sat beside it. */
            variant_name: string;
            /** @description A JSON NUMBER - a small retail count, unlike money and unlike a stock count. */
            quantity: number;
            unit_price: components["schemas"]["MinorAmount"];
            /** @description `"0"` when `unavailable` is true. */
            line_total: string;
            /** @description ALWAYS `"0"`. There are no per-line discounts. */
            discount_total: string;
            /** @description The variant can no longer be sold. The line stays visible, contributes `"0"` to every total, and BLOCKS CHECKOUT until it is removed. */
            unavailable: boolean;
        };
        /** @description Every cart response and every cart mutation returns this whole object, priced. A storefront never adds anything up. */
        Cart: {
            /** Format: uuid */
            public_id: string;
            /**
             * @description COMPUTED - reads `expired` the moment `expires_at` has passed, whatever the stored column says. Nothing sweeps carts.
             * @enum {string}
             */
            status: "open" | "converted" | "expired";
            currency: components["schemas"]["Currency"];
            /** Format: uuid */
            customer_id: string | null;
            session_id: string | null;
            /** @description Insertion order. */
            items: components["schemas"]["CartLine"][];
            coupon_code: string | null;
            /** @description BRANCH ON THIS. A code set with `coupon_applied: false` is a lapsed, withdrawn, sold-out or below-minimum campaign. */
            coupon_applied: boolean;
            /**
             * Format: uuid
             * @description Null when none is chosen OR when the chosen one has been withdrawn.
             */
            shipping_method_id: string | null;
            subtotal: components["schemas"]["MinorAmount"];
            discount_total: components["schemas"]["MinorAmount"];
            shipping_total: components["schemas"]["MinorAmount"];
            /** @description CONTAINED IN `grand_total`, not added to it. Adding them double-counts. */
            tax_total: string;
            /** @description `max(0, subtotal - discount_total + shipping_total)`. */
            grand_total: string;
            /** @description Branch on this, never on the totals. When true, `subtotal` does NOT equal the sum of the lines. */
            has_unavailable_items: boolean;
            /**
             * Format: date-time
             * @description 30 days from creation, and NEVER extended by a mutation.
             */
            expires_at: string;
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            updated_at: string;
        };
        /** @description `currency` is absent on purpose - a cart copies the shop's and may not choose. */
        CartCreate: {
            /**
             * Format: uuid
             * @description An opaque reference to YOUR user. Never validated, never resolved.
             */
            customer_id?: string | null;
            /** @description Your own anonymous key. NEVER used for scoping. */
            session_id?: string | null;
        };
        CartItemCreate: {
            /**
             * Format: uuid
             * @description A VARIANT's public_id. Another shop's answers 422, not 404.
             */
            variant_id: string;
            /** @description The amount to ADD. The 99 cap applies to the resulting LINE. */
            quantity: number;
        };
        CartItemUpdate: {
            /** @description Absolute, not a delta - which is what makes this safe to retry. */
            quantity: number;
        };
        CartCoupon: {
            code: string;
        };
        CartShippingMethod: {
            /**
             * Format: uuid
             * @description The key must be PRESENT. Null clears the choice.
             */
            shipping_method_id: string | null;
        };
        /** @description Strict, and stored exactly as sent so the readback is byte-identical. */
        Address: {
            name: string;
            line1: string;
            line2?: string | null;
            city: string;
            /** @description Free text. NO format validation of any kind. */
            postal_code: string;
            /** @description ISO 3166-1 alpha-2, UPPER CASE. `"hu"` is a 400. */
            country: string;
            /** @description Free text. No format validation. */
            phone?: string | null;
        };
        /** @description AT LEAST ONE of `customer_id` and `guest_email` must be present, or the 400 carries TWO issues, one on each pointer. Nothing about money is here - totals come from the cart. */
        CheckoutRequest: {
            /** Format: uuid */
            customer_id?: string | null;
            /**
             * Format: email
             * @description The only place this service ever holds an email address.
             */
            guest_email?: string | null;
            shipping_address: components["schemas"]["Address"];
            /** @description Null means "the same as shipping". THE SERVICE DOES NOT COPY IT - you resolve that. */
            billing_address?: components["schemas"]["Address"] | null;
        };
        /** @description Copied at checkout. The catalog may say something else today; this line may not. */
        OrderLine: {
            /**
             * Format: uuid
             * @description For a link back to the product page - never for a price.
             */
            product_id: string;
            /** Format: uuid */
            variant_id: string;
            product_name: string;
            variant_name: string;
            sku: string | null;
            unit_price: components["schemas"]["MinorAmount"];
            quantity: number;
            /** @description Always `"0"`. */
            discount_total: string;
            total: components["schemas"]["MinorAmount"];
            currency: components["schemas"]["Currency"];
        };
        /**
         * @description `canceled` has ONE `l` - the estate spelling. EVERY status is now reachable, but only `fulfilled` and `canceled` are reachable BY AN API CALL: `paid`, `confirmed` and `refunded` arrive exclusively from payment-service, through the inbound receiver or the reconcile poll.
         *     `paid` is a STEP AND NOT A DESTINATION - the payment-success handler moves `pending -> paid`, commits the stock, and moves `paid -> confirmed` in the SAME transaction, so you will almost never observe it. Wait for `confirmed`; do not build a screen or a branch that waits for `paid`. Treat an unrecognised status as "in progress, do not act".
         * @enum {string}
         */
        OrderStatus: "pending" | "paid" | "confirmed" | "fulfilled" | "payment_failed" | "canceled" | "refunded";
        /** @description Immutable history. Every value a buyer was shown is copied here at checkout, and no order read joins the live catalog. The admin tier serves this identical shape. */
        Order: {
            /** Format: uuid */
            public_id: string;
            status: components["schemas"]["OrderStatus"];
            currency: components["schemas"]["Currency"];
            /** Format: uuid */
            customer_id: string | null;
            guest_email: string | null;
            items: components["schemas"]["OrderLine"][];
            subtotal: components["schemas"]["MinorAmount"];
            discount_total: components["schemas"]["MinorAmount"];
            shipping_total: components["schemas"]["MinorAmount"];
            /** @description CONTAINED IN `grand_total`, not added to it. */
            tax_total: string;
            grand_total: components["schemas"]["MinorAmount"];
            /** @description Copied, not joined. Null when the order needs no carriage. */
            shipping_method_name: string | null;
            shipping_method_price: string | null;
            coupon_code: string | null;
            coupon_discount: string | null;
            shipping_address: components["schemas"]["Address"];
            /** @description Null means "the same as shipping" - the same rule as on the request. */
            billing_address: components["schemas"]["Address"] | null;
            /**
             * @description payment-service's payment `public_id`, written after the checkout transaction commits. Null until the payment is created, and PERMANENTLY NULL for a shop holding no credential.
             *     A `pending` order with a null `payment_ref` IS NOT RECONCILABLE - the reconcile poll only touches orders that have one. What recovers such an order is the buyer retrying the checkout with the same Idempotency-Key.
             */
            payment_ref: string | null;
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            updated_at: string;
        };
        OrderList: components["schemas"]["ListEnvelope"] & {
            data?: components["schemas"]["Order"][];
        };
        /** @description PAYMENT-SERVICE'S RESOURCE, RE-PUBLISHED VERBATIM. This service does not own payments, so nothing here is re-derived or translated - doing so would put a second opinion about a payment in a service that has none. */
        Payment: {
            /** @description payment-service's payment id. Also written to the order's `payment_ref`. */
            public_id: string;
            /** @description PAYMENT-SERVICE'S VOCABULARY, not this service's order-status enum, and deliberately NOT validated against a list here. Treat it as opaque display data and never map it onto an order status. */
            status: string;
            /** @description Where to redirect the buyer. NULL MEANS DO NOT REDIRECT - payment-service's contract, passed through unchanged. Handle it exactly as you handle a null `payment`. */
            gateway_url: string | null;
        };
        CheckoutResponse: components["schemas"]["Order"] & {
            /**
             * @description NULL WHEN THE SHOP HOLDS NO PAYMENT CREDENTIAL - a configuration state, not a failure. The order still commits `pending` with its stock held and the 201 is still a 201; an operator finds the shop via `shops_without_payment_credential` on admin health.
             *     THIS MEMBER EXISTS ONLY ON THE CHECKOUT RESPONSE. `GET /v1/orders/{id}` returns `payment_ref` and no `payment` block, because the block is payment-service's resource fetched at that one instant rather than something this service stores.
             */
            payment: components["schemas"]["Payment"] | null;
        };
        PaymentCredentialRequest: {
            /**
             * @description payment-service's key IN FULL, INCLUDING THE `pmk_` PREFIX. The prefix is validated rather than assumed: a key pasted without it authenticates against nothing, and the first symptom would be every checkout in that shop answering 502 hours later.
             * @example pmk_YOUR_PAYMENT_SERVICE_KEY
             */
            secret: string;
        };
        /** @description The whole readback. There is no route that returns the plaintext (HR-18), and the absence is the design rather than an omission. */
        PaymentCredential: {
            /** Format: uuid */
            public_id: string;
            secret_last4: string;
            secret_fingerprint: string;
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            updated_at: string;
        };
        EventType: {
            /**
             * @description NOTE `order.canceled` HAS ONE `l` - the estate's spelling, fixed in the status enum from migration 0000 precisely so this name never needed a rename. There is deliberately NO `order.paid`: `paid` is a step inside one transaction, not a destination, and the outcome a receiver acts on is `order.confirmed` moments later.
             * @enum {string}
             */
            event_type: "order.created" | "order.confirmed" | "order.payment_failed" | "order.canceled" | "order.fulfilled" | "order.refunded";
            /**
             * @description A `transition` means `orders.status` moved and the status invariant binds: `data.order.status` EQUALS `target_status`. An `observation` makes no such promise - `order.created` is the one, so A RECEIVER MUST BRANCH ON `data.order.status`, NEVER ON THE EVENT NAME.
             * @enum {string}
             */
            kind: "transition" | "observation";
            /** @constant */
            resource: "order";
            fires_when: string;
            /** @description Null on an observation. */
            target_status: string | null;
            /** @description Which `data` blocks are omitted unless the endpoint has `include_customer: true`. Named per type so an operator UI can warn before the box is ticked (WH-11). */
            sensitive_blocks: string[];
            /** @description Whether another service in the estate is registered to act on it. */
            cross_service: boolean;
        };
        EventTypeList: components["schemas"]["ListEnvelope"] & {
            data?: components["schemas"]["EventType"][];
        };
        WebhookEndpointCreate: {
            /**
             * Format: uri
             * @description HTTPS. Checked for deliverability at save time AND AGAIN at delivery, because a save proves only what was true at one instant. A rejected URL is a 400 with `pointer: "/url"`.
             */
            url: string;
            /** @description The operator's note. What the listing shows. */
            description?: string | null;
            /** @description NULL MEANS EVERY TYPE IN THE CATALOGUE. An EMPTY ARRAY IS A 400, never "none" - a subscription to nothing is a mistake, not a configuration (WH-7). */
            subscribed_events?: string[] | null;
            /**
             * @description OFF BY DEFAULT, and leave it off unless you need it. A webhook body does not stay in one place: it lands in the receiver's request log, in whatever drain they run, in every proxy between us, and in a support ticket the first time something goes wrong. Enabling it writes a dedicated audit row.
             * @default false
             */
            include_customer: boolean;
        };
        /** @description Every member optional. An absent member is unchanged. */
        WebhookEndpointUpdate: {
            /** Format: uri */
            url?: string;
            description?: string | null;
            subscribed_events?: string[] | null;
            include_customer?: boolean;
        };
        WebhookEndpoint: {
            /** Format: uuid */
            public_id: string;
            /** Format: uri */
            url: string;
            description?: string | null;
            /** @description Null means every type in the catalogue. */
            subscribed_events: string[] | null;
            /** @description Pinned at create. A delivery is rendered down to this version to reach the endpoint. */
            contract_version: number;
            enabled: boolean;
            /** @description A disabled endpoint ALWAYS says why - a check constraint requires it - so an operator reading the list needs no second query. Human-readable, and never the thing a count is derived from. */
            disabled_reason?: string | null;
            /** @description Exhausted LADDERS, not attempts. At 5 the endpoint auto-disables. Re-enabling clears this and the reason together. */
            consecutive_failures?: number;
            include_customer: boolean;
            /** @description The readback, and the whole readback. The plaintext is shown once, at mint. */
            secret_last4: string;
            secret_fingerprint?: string;
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            updated_at: string;
        };
        MintedWebhookEndpoint: components["schemas"]["WebhookEndpoint"] & {
            /**
             * @description THE PLAINTEXT SIGNING SECRET, RETURNED EXACTLY ONCE - here on create and on rotate-secret, and by no read route ever. Capture it now. A lost secret is rotated, not recovered, and rotating invalidates every signature the receiver is currently verifying.
             * @example whsec_YOUR_ENDPOINT_SECRET
             */
            secret: string;
        };
        WebhookEndpointList: components["schemas"]["ListEnvelope"] & {
            data?: components["schemas"]["WebhookEndpoint"][];
        };
        /** @description Immutable. Written once, inside the transaction that made the fact true. */
        WebhookEvent: {
            /**
             * Format: uuid
             * @description THIS IS THE `event_id` ON THE WIRE and in `X-Event-Id`. One id, not two - dedupe on it.
             */
            public_id: string;
            event_type: string;
            contract_version: number;
            /** @description The frozen body, exactly as sent to every attempt of every redelivery. `data.customer` is STRIPPED from this listing regardless of any endpoint's `include_customer`. */
            payload?: {
                [key: string]: unknown;
            };
            /**
             * Format: uuid
             * @description The shop's `external_ref` at emission. NULL IS LEGITIMATE and is counted on admin health - a null makes the event unroutable at a receiving service.
             */
            account_id?: string | null;
            /**
             * Format: uuid
             * @description Equals `public_id` on a natively produced event.
             */
            correlation_id: string;
            /**
             * Format: uuid
             * @description The inbound event that caused this one, or null when nothing did. Null on the reconcile path is the TRUTH rather than a gap: a poll found a fact nobody told us.
             */
            causation_id?: string | null;
            hop: number;
            /**
             * Format: date-time
             * @description WHEN THE FACT BECAME TRUE, not when it was sent. The `from`/`until` filters test this column, unlike every other listing in this spec.
             */
            occurred_at: string;
            /** Format: date-time */
            created_at: string;
        };
        WebhookEventList: components["schemas"]["ListEnvelope"] & {
            data?: components["schemas"]["WebhookEvent"][];
        };
        /** @description ONE ROW PER (event, endpoint) PAIR, never two. A redelivery RESETS this row. content-service held "a re-fire is a new event" and "dedupe on X-Event-Id" - each defensible alone - and together they made an operator's re-fire indistinguishable from a second publish, so receivers processed it twice. The unique index is the fix. */
        WebhookDelivery: {
            /**
             * Format: uuid
             * @description The ROW's id. Not `X-Delivery-Id`, which is regenerated per attempt.
             */
            public_id: string;
            /** Format: uuid */
            event_id?: string;
            /** Format: uuid */
            endpoint_id?: string;
            event_type?: string;
            /** Format: uri */
            url?: string;
            /** @enum {string} */
            status: "pending" | "delivered" | "dead_lettered";
            /** @description 0-8. At 8 exhausted attempts the row becomes `dead_lettered`. */
            attempt: number;
            /**
             * Format: date-time
             * @description A FLOOR, NOT A SCHEDULE. The rung says "not before"; what turns it into "at about" is drain frequency, and the scheduled drain runs ONCE A DAY on this deployment.
             */
            next_attempt_at?: string | null;
            /** @description The receiver's HTTP status, or null when there was no response at all. */
            response_status?: number | null;
            /** @description At most 500 bytes, truncated WHILE READING - a 10 MB error page is not our problem. */
            response_excerpt?: string | null;
            /**
             * @description Why an attempt produced NO status code at all. A 4xx is a status, not one of these, so `error` and `response_status` are never both set.
             * @enum {string|null}
             */
            error?: "timeout" | "redirect_not_followed" | "url_rejected" | null;
            /** Format: date-time */
            delivered_at?: string | null;
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            updated_at: string;
        };
        WebhookDeliveryList: components["schemas"]["ListEnvelope"] & {
            data?: components["schemas"]["WebhookDelivery"][];
        };
        /** @description The synchronous outcome of a `webhook.ping`. A 200 from the route means the CALL WAS MADE, not that the receiver accepted it - read these members. */
        PingResult: {
            delivered?: boolean;
            response_status?: number | null;
            /** @enum {string|null} */
            error?: "timeout" | "redirect_not_followed" | "url_rejected" | null;
            response_excerpt?: string | null;
        };
        Job: {
            /** Format: uuid */
            public_id: string;
            /**
             * Format: uuid
             * @description NULL FOR PLATFORM WORK: the sweeps belong to every shop and to none.
             */
            shop_id?: string | null;
            /** @enum {string} */
            type: "webhook.deliver" | "inventory.release_expired" | "cart.expire" | "order.confirm" | "order.fail_payment" | "order.refund" | "payment.reconcile";
            /**
             * @description `due` is a FILTER, not a status: it is the drain's eligibility predicate. A `claimed` job whose lease has passed is eligible again with no writer at all, which is why no sweeper exists to forget to run.
             * @enum {string}
             */
            status: "pending" | "claimed" | "done" | "dead";
            attempts: number;
            /** @description 8 for deliveries, 5 for the inbound actions, 3 for the sweeps and reconcile. */
            max_attempts: number;
            /**
             * Format: date-time
             * @description "Not before". The daily drain is what decides "at about".
             */
            run_after: string;
            /**
             * Format: date-time
             * @description The lease. Past it, the job is re-eligible.
             */
            claimed_until?: string | null;
            /** @description Truncated to 1 000 characters. Read this before retrying. */
            last_error?: string | null;
            /** Format: date-time */
            canceled_at?: string | null;
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            updated_at: string;
        };
        JobList: components["schemas"]["ListEnvelope"] & {
            data?: components["schemas"]["Job"][];
        };
        /** @description A ledger row. NO PAYLOAD IS STORED - see the listing's description. */
        InboundEvent: {
            /** Format: uuid */
            public_id: string;
            /**
             * @description The emitter's slug. Never a URL, never a host.
             * @constant
             */
            source_service: "payment-service";
            /**
             * Format: uuid
             * @description THE EMITTER'S id - the one a cross-service query joins on, and what makes a duplicate delivery a 200 and a no-op.
             */
            event_id: string;
            event_type: string;
            /**
             * @description `accepted` means recorded AND queued; the order moves when the job runs. `ignored` means recorded and deliberately not acted on. `refused` means recorded and rejected - the hop limit and the replay window reach it.
             * @enum {string}
             */
            outcome: "accepted" | "ignored" | "refused";
            /**
             * @description A CLOSED set, never prose - a count derived from a sentence keeps passing until somebody rewords the sentence, and then silently counts zero. Null on an `accepted` row.
             * @enum {string|null}
             */
            reason_code: "unknown_account" | "unknown_order" | "hop_limit" | "unsupported_type" | "already_handled" | "stale" | null;
            /** Format: uuid */
            correlation_id?: string | null;
            /** Format: uuid */
            causation_id?: string | null;
            hop: number;
            /** Format: date-time */
            received_at: string;
        };
        InboundEventList: components["schemas"]["ListEnvelope"] & {
            data?: components["schemas"]["InboundEvent"][];
        };
        /** @description payment-service's envelope. `additionalProperties: true` IS THE CONTRACT, not laxity: here an unknown member is the emitter adding one, and refusing it would turn their additive change into our outage. Only the members this service actually reads are named. */
        InboundEnvelope: {
            /**
             * Format: uuid
             * @description The emitter's id, and the dedupe key.
             */
            event_id: string;
            /** @description Only `payment.succeeded`, `payment.failed`, `payment.expired` and `refund.succeeded` cause an action. ANY OTHER TYPE IS RECORDED `ignored` WITH `unsupported_type` AND ANSWERED 200 - decided by a closed map's absence of a key rather than by a branch somebody must remember to write. */
            event_type: string;
            /** Format: date-time */
            occurred_at: string;
            service: string;
            /**
             * Format: uuid
             * @description THE LAMIDO-ADMIN ACCOUNT ID, AND THE ONLY WAY THE SHOP IS RESOLVED (WH-23). A null, or one matching no shop's `external_ref`, is recorded `ignored` / `unknown_account` and acted on by nothing - silently, with a 200.
             */
            account_id: string | null;
            /** Format: uuid */
            correlation_id: string;
            /** Format: uuid */
            causation_id: string | null;
            /** @description At or above MAX_HOP = 3 the event is recorded `refused` / `hop_limit`. The hop check runs BEFORE the shop lookup, so a cycle is refused even for a shop we know. The replay window runs before BOTH: an `X-Signature-Timestamp` more than 7 days old, or more than 5 minutes ahead of our clock, is recorded `refused` / `stale` and answered 200. The window sits BESIDE the dedupe ledger and never instead of it. */
            hop: number;
            /**
             * @description The order is found via `data.payment.merchant_payment_ref`, falling back to `data.refund.merchant_payment_ref` - which is our order's `public_id`, echoed back from the create call. An event carrying neither is `ignored` / `unknown_order`.
             * @default {}
             */
            data: {
                [key: string]: unknown;
            };
        } & {
            [key: string]: unknown;
        };
        InboundReceipt: {
            /**
             * @description ALWAYS TRUE ON A 200, INCLUDING FOR EVENTS WE IGNORED. It means "received", not "acted on": read `outcome`.
             * @constant
             */
            received: true;
            /** @enum {string} */
            outcome: "accepted" | "ignored" | "refused";
            /** @enum {string|null} */
            reason_code: "unknown_account" | "unknown_order" | "hop_limit" | "unsupported_type" | "already_handled" | "stale" | null;
        };
        /**
         * @description WHO acted. `system` is the service itself - a sweep, a job, a reconcile - and it is the value that makes a report spanning both tiers complete rather than partial.
         * @enum {string}
         */
        AuditActorType: "admin_key" | "shop_key" | "customer" | "system";
        /** @description One mutation. APPEND-ONLY and never edited, with NO FOREIGN KEYS: the history has to outlive the key that made it. */
        AuditRow: {
            /**
             * Format: uuid
             * @description The row's id, and the pager's tiebreaker. It exists because of that: the internal bigint must not leave the process (HR-9), so keyset paging over this table needed a column added in migration `0007` before the read surface could exist at all.
             */
            public_id: string;
            actor_type: components["schemas"]["AuditActorType"];
            /** @description The acting key's `public_id`, or null for `system`. */
            actor_id?: string | null;
            /** @description The key's label as it was AT THE TIME. A renamed key does not rewrite history. */
            actor_label: string;
            /** @description e.g. `order.checkout`, `shop.create`, `webhook_endpoint.rotate_secret`. */
            action: string;
            target_type?: string | null;
            target_id?: string | null;
            /** @description WHICH KEYS CHANGED, never their values, allowlisted per action on the way in. There is no amount, no address and no email on any row - a trail that carries the money and the buyer is a second copy of the order, with none of its guarantees. */
            metadata?: {
                [key: string]: unknown;
            };
            ip?: string | null;
            /** @description Joins this row to the log lines of the request that wrote it. */
            request_id?: string | null;
            /** Format: date-time */
            created_at: string;
        };
        AuditRowList: components["schemas"]["ListEnvelope"] & {
            data?: components["schemas"]["AuditRow"][];
        };
        /** @description What one drain did. `x-internal` - no consumer reads this. */
        DrainTickResult: {
            claimed: number;
            succeeded: number;
            failed: number;
            /** @description True when the wall budget stopped the loop rather than the queue running dry. The remaining jobs keep their lease and become eligible again when it expires, so nothing is lost and nothing needs un-sticking. */
            budget_exhausted: boolean;
        };
        SweepsTickResult: components["schemas"]["DrainTickResult"] & {
            /**
             * @description The sweep job types this tick actually added - FEWER THAN FOUR when a previous tick's sweep is still live, because each holds one job at a time under a unique key.
             * @example [
             *       "inventory.release_expired",
             *       "cart.expire"
             *     ]
             */
            enqueued: string[];
        };
    };
    responses: {
        /** @description 400. A body, query parameter or required header failed validation. `errors[]` names EVERY offending field at once, so one round trip fixes them all. */
        ValidationError: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description 401. Missing, malformed, unknown or REVOKED key, or a SUSPENDED shop - the bodies are byte-identical, on purpose. There is no `code` member. */
        Unauthorized: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description 403. The browser tripwire, the wrong key tier, a missing scope (named in `detail`), or an IP allowlist. Informative, unlike a 401. */
        Forbidden: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description 404. No such row IN YOUR SCOPE, no such endpoint, or a malformed identifier. Never 403 for a cross-tenant read - that would confirm the row exists. */
        NotFound: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description 409. `idempotency_in_flight` (another attempt holds the 60 s lease - RETRY in a moment) or `idempotency_key_reused` (the same key with a different body - a client bug). */
        IdempotencyConflict: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description 422. The request is well formed and the STATE refuses it. Branch on `code`. Most are fixable and, on checkout, all of them release the idempotency key. */
        StateRefusal: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description 429. A throttle bucket is full. `retry_after` is in the body and `Retry-After` in the header, derived from it so they cannot disagree. */
        RateLimited: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description 500. The real message is logged, never returned. Quote `request_id`. */
        InternalError: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description 429 from the PER-SHOP payment-create bucket (60 per shop per 60 s), consumed before the credential is read and before any upstream call - so NO PAYMENT WAS CREATED. But the checkout transaction had already committed, so this DID leave a real `pending` order holding stock. Honour `Retry-After`, then retry the SAME Idempotency-Key: that takes the resume path. */
        PaymentThrottled: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description 502 with a `provider_error` and NO `code`. Carries `type: "urn:webshop-service:problem:internal"`, because the type set is closed. Both variants leave a real `pending` order holding stock: `payment_create_rejected` is definitive and must not be retried until the shop's configuration changes; `payment_create_unknown` means a payment may or may not exist and MUST be retried with the same Idempotency-Key. */
        ProviderError: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description 409 `endpoint_limit_reached`. The shop already holds 20 webhook endpoints (WH-6). The body is well formed; what refuses it is how many rows exist. Delete one first. */
        EndpointLimit: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description 400, the ONLY 4xx this receiver produces. `errors[0].code` is `invalid_signature` (pointer `#/header/X-Signature`) or `invalid_envelope` (pointer `#/body`). It is a validation problem rather than a 401 deliberately: a 401 invites a client to go and find credentials, and there are none to find - what failed is the body's own integrity. NOTHING IS STORED on either path. */
        InboundRejected: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
    };
    parameters: {
        /** @description 1-200. Out of range is a 400, never a clamp. */
        limit: number;
        /** @description Opaque keyset cursor from a previous `next_cursor`. DO NOT PARSE OR CONSTRUCT ONE. Malformed is a 400, never a silent restart at page 1. */
        cursor: string;
        /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
        publicId: string;
        /** @description A cart line's `public_id`, from the cart's `items[].public_id`. */
        itemPublicId: string;
        /** @description A shop key's `public_id`. */
        keyId: string;
        /** @description The product's `public_id` or its slug. A UUID-shaped segment is tried as an id; anything else can only be a slug. */
        idOrSlug: string;
        /** @description 1-255 characters. REQUIRED on checkout and accepted nowhere else. One key per INTENT: a new key is a new order, and every 400 or 422 releases the key so the same one may be reused. */
        idempotencyKey: string;
        /** @description A previously returned ETag. Compared weakly; `*` matches anything. */
        ifNoneMatch: string;
        /** @description INCLUSIVE lower bound on `created_at`. Malformed is a 400 - STRICT ISO 8601, either a full instant with an offset or a bare `YYYY-MM-DD`, which reads as UTC midnight. `"March 5 2026"` and `"2026"` are refused rather than guessed at. */
        from: string;
        /** @description EXCLUSIVE upper bound on `created_at`. Malformed is a 400 - STRICT ISO 8601, either a full instant with an offset or a bare `YYYY-MM-DD`, which reads as UTC midnight. `"March 5 2026"` and `"2026"` are refused rather than guessed at. */
        until: string;
        /** @description An unknown value IS a 400 on the order listings. */
        orderStatus: "pending" | "paid" | "confirmed" | "fulfilled" | "payment_failed" | "canceled" | "refunded";
        /** @description An unrecognised value is SILENTLY IGNORED and returns an unfiltered list. */
        shopStatus: "active" | "suspended";
        /** @description An unrecognised value is SILENTLY IGNORED and returns an unfiltered list. */
        productStatus: "draft" | "published" | "archived";
        /** @description Only the literal strings `true` and `false` filter. Anything else is ignored. */
        activeFilter: "true" | "false";
        /** @description Only the literal string `true` filters. Restricts to ACTIVE shops with a null `external_ref` - the same predicate as the admin-health count. */
        withoutExternalRef: "true";
        /** @description A shop's `public_id`. Unknown is a 404. Omitting it returns every shop's rows. */
        shopFilter: string;
        /** @description Only the literal string `true` filters. The same predicate as the admin-health count. DELIBERATELY NOT COMBINABLE with `without_external_ref`: each drills into exactly one number and an AND of both would drill into neither, so passing both applies only this one. */
        withoutPaymentCredential: "true";
        /** @description Only the literal string `true` filters, through the same predicate as the `oversold_orders` health count. IT IGNORES `?status=` rather than intersecting with it - an oversold order is `confirmed` or `fulfilled` by definition, so an AND with `?status=pending` would answer an empty list to a question nobody meant to ask. */
        oversoldFilter: "true";
        /** @description A webhook endpoint's `public_id`. NOTE there is no shop segment on these routes - the shop is read off the endpoint itself, because cross-shop reach is what a `wad_` key is for. */
        endpointId: string;
        /** @description A webhook delivery ROW's `public_id`. Not `X-Delivery-Id`, which changes per attempt. */
        deliveryId: string;
        /** @description A job's `public_id`. */
        jobId: string;
        /** @description A webhook endpoint's `public_id`. Unknown is a 404. */
        endpointFilter: string;
        /** @description A TRI-STATE: absent, `true` or `false`. Anything else is a 400, never a silent "absent" - unlike the tolerant shop and product filters. `false` uses the same predicate as the `disabled_endpoints` health count. */
        enabledFilter: "true" | "false";
        /** @description One of the six catalogue types. An unknown value IS a 400. */
        eventTypeFilter: "order.created" | "order.confirmed" | "order.payment_failed" | "order.canceled" | "order.fulfilled" | "order.refunded";
        /** @description An unknown value IS a 400. `pending` and `dead_lettered` use the same predicates as the two delivery health counts. */
        deliveryStatusFilter: "pending" | "delivered" | "dead_lettered";
        /** @description One of the seven job types. An unknown value IS a 400. */
        jobTypeFilter: "webhook.deliver" | "inventory.release_expired" | "cart.expire" | "order.confirm" | "order.fail_payment" | "order.refund" | "payment.reconcile";
        /** @description An unknown value IS a 400. NOTE `due` is NOT a column value - it is the drain's own eligibility predicate (pending, not canceled, `run_after` in the past), and it is the filter that matches the `due_jobs` health count. `dead` also uses the health predicate, which EXCLUDES canceled rows; a plain column equality would have included them. */
        jobStatusFilter: "pending" | "claimed" | "done" | "dead" | "due";
        /** @description Only `unknown_account` filters, and it is the `unroutable_inbound_events` drill-down. ANYTHING ELSE IS IGNORED rather than refused, so a typo returns the unfiltered list. */
        reasonCodeFilter: "unknown_account";
        /** @description Only `refused` filters, and it is the `refused_inbound_events` drill-down. Anything else is IGNORED rather than refused. */
        outcomeFilter: "refused";
        /** @description A CLOSED SET, and an unknown value is a 400 rather than an empty page - HR-19's last MUST, because an empty page reads as "nothing happened". */
        actorTypeFilter: components["schemas"]["AuditActorType"];
        /** @description Exact match on the action name, e.g. `order.checkout`. NOT a prefix and not a pattern; the action vocabulary grows per phase and is listed in `operations.md`. */
        auditActionFilter: string;
        /** @description The kind of resource acted on, e.g. `order`, `shop`, `product`, `webhook_endpoint`. */
        targetTypeFilter: string;
        /** @description REQUIRES `target_type` - an id alone does not identify a target, and the unpaired query would scan without an index and return another kind's history. Alone it is a 400. */
        targetIdFilter: string;
    };
    requestBodies: never;
    headers: {
        /** @description A weak-compared validator. Send it back as `If-None-Match`. */
        ETag: string;
        /** @description `public, s-maxage=60, stale-while-revalidate=300`, or `public, s-maxage=10` on a 404. */
        CacheControl: string;
        /** @description Always `Origin, Authorization` on the public tier, including on a 401. */
        Vary: string;
        /** @description Present and `"true"` only when the response is a replay of a frozen body. */
        IdempotentReplay: string;
    };
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    getLiveness: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Alive. Read the body for the diagnosis. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Liveness"];
                };
            };
        };
    };
    listPublicProducts: {
        parameters: {
            query?: {
                /** @description 1-200. Out of range is a 400, never a clamp. */
                limit?: components["parameters"]["limit"];
                /** @description Opaque keyset cursor from a previous `next_cursor`. DO NOT PARSE OR CONSTRUCT ONE. Malformed is a 400, never a silent restart at page 1. */
                cursor?: components["parameters"]["cursor"];
            };
            header?: {
                /** @description A previously returned ETag. Compared weakly; `*` matches anything. */
                "If-None-Match"?: components["parameters"]["ifNoneMatch"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of published products. */
            200: {
                headers: {
                    ETag: components["headers"]["ETag"];
                    "Cache-Control": components["headers"]["CacheControl"];
                    Vary: components["headers"]["Vary"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PublicProductList"];
                };
            };
            /** @description The caller's validator matches. Empty body. */
            304: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["ValidationError"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            429: components["responses"]["RateLimited"];
            500: components["responses"]["InternalError"];
        };
    };
    getPublicProduct: {
        parameters: {
            query?: never;
            header?: {
                /** @description A previously returned ETag. Compared weakly; `*` matches anything. */
                "If-None-Match"?: components["parameters"]["ifNoneMatch"];
            };
            path: {
                /** @description The product's `public_id` or its slug. A UUID-shaped segment is tried as an id; anything else can only be a slug. */
                id_or_slug: components["parameters"]["idOrSlug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The product. */
            200: {
                headers: {
                    ETag: components["headers"]["ETag"];
                    "Cache-Control": components["headers"]["CacheControl"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PublicProduct"];
                };
            };
            /** @description The caller's validator matches. Empty body. */
            304: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["RateLimited"];
        };
    };
    getStorefrontSelf: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The shop and the key presented. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["StorefrontSelf"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listStorefrontProducts: {
        parameters: {
            query?: {
                /** @description 1-200. Out of range is a 400, never a clamp. */
                limit?: components["parameters"]["limit"];
                /** @description Opaque keyset cursor from a previous `next_cursor`. DO NOT PARSE OR CONSTRUCT ONE. Malformed is a 400, never a silent restart at page 1. */
                cursor?: components["parameters"]["cursor"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of published products. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PublicProductList"];
                };
            };
            400: components["responses"]["ValidationError"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    getStorefrontProduct: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The product's `public_id` or its slug. A UUID-shaped segment is tried as an id; anything else can only be a slug. */
                id_or_slug: components["parameters"]["idOrSlug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The product. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PublicProduct"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    createCart: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: {
            content: {
                "application/json": components["schemas"]["CartCreate"];
            };
        };
        responses: {
            /** @description The new cart, priced, with an empty `items` array. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Cart"];
                };
            };
            400: components["responses"]["ValidationError"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    getCart: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The cart, priced. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Cart"];
                };
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
        };
    };
    addCartItem: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CartItemCreate"];
            };
        };
        responses: {
            /** @description The whole cart, priced. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Cart"];
                };
            };
            400: components["responses"]["ValidationError"];
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            422: components["responses"]["StateRefusal"];
        };
    };
    deleteCartItem: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
                /** @description A cart line's `public_id`, from the cart's `items[].public_id`. */
                item_public_id: components["parameters"]["itemPublicId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The whole cart, priced. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Cart"];
                };
            };
            404: components["responses"]["NotFound"];
            422: components["responses"]["StateRefusal"];
        };
    };
    setCartItemQuantity: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
                /** @description A cart line's `public_id`, from the cart's `items[].public_id`. */
                item_public_id: components["parameters"]["itemPublicId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CartItemUpdate"];
            };
        };
        responses: {
            /** @description The whole cart, priced. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Cart"];
                };
            };
            400: components["responses"]["ValidationError"];
            404: components["responses"]["NotFound"];
            422: components["responses"]["StateRefusal"];
        };
    };
    applyCartCoupon: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CartCoupon"];
            };
        };
        responses: {
            /** @description The whole cart, priced, with the coupon applied. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Cart"];
                };
            };
            400: components["responses"]["ValidationError"];
            404: components["responses"]["NotFound"];
            422: components["responses"]["StateRefusal"];
        };
    };
    removeCartCoupon: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The whole cart, priced. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Cart"];
                };
            };
            404: components["responses"]["NotFound"];
            422: components["responses"]["StateRefusal"];
        };
    };
    listCartShippingOptions: {
        parameters: {
            query?: {
                /** @description 1-200. Out of range is a 400, never a clamp. */
                limit?: components["parameters"]["limit"];
                /** @description Opaque keyset cursor from a previous `next_cursor`. DO NOT PARSE OR CONSTRUCT ONE. Malformed is a 400, never a silent restart at page 1. */
                cursor?: components["parameters"]["cursor"];
            };
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of shipping options. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ShippingOptionList"];
                };
            };
            400: components["responses"]["ValidationError"];
            404: components["responses"]["NotFound"];
        };
    };
    setCartShippingMethod: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CartShippingMethod"];
            };
        };
        responses: {
            /** @description The whole cart, priced, with `shipping_total` folded in. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Cart"];
                };
            };
            400: components["responses"]["ValidationError"];
            404: components["responses"]["NotFound"];
            422: components["responses"]["StateRefusal"];
        };
    };
    checkoutCart: {
        parameters: {
            query?: never;
            header: {
                /** @description 1-255 characters. REQUIRED on checkout and accepted nowhere else. One key per INTENT: a new key is a new order, and every 400 or 422 releases the key so the same one may be reused. */
                "Idempotency-Key": components["parameters"]["idempotencyKey"];
            };
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CheckoutRequest"];
            };
        };
        responses: {
            /** @description The order with `status: "pending"`, plus a `payment` block - which is `null` when the shop holds no payment credential. On a replay the bytes are identical. */
            201: {
                headers: {
                    "Idempotent-Replay": components["headers"]["IdempotentReplay"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CheckoutResponse"];
                };
            };
            400: components["responses"]["ValidationError"];
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["IdempotencyConflict"];
            422: components["responses"]["StateRefusal"];
            429: components["responses"]["PaymentThrottled"];
            500: components["responses"]["InternalError"];
            502: components["responses"]["ProviderError"];
        };
    };
    listStorefrontOrders: {
        parameters: {
            query?: {
                /** @description 1-200. Out of range is a 400, never a clamp. */
                limit?: components["parameters"]["limit"];
                /** @description Opaque keyset cursor from a previous `next_cursor`. DO NOT PARSE OR CONSTRUCT ONE. Malformed is a 400, never a silent restart at page 1. */
                cursor?: components["parameters"]["cursor"];
                /** @description An unknown value IS a 400 on the order listings. */
                status?: components["parameters"]["orderStatus"];
                /** @description INCLUSIVE lower bound on `created_at`. Malformed is a 400 - STRICT ISO 8601, either a full instant with an offset or a bare `YYYY-MM-DD`, which reads as UTC midnight. `"March 5 2026"` and `"2026"` are refused rather than guessed at. */
                from?: components["parameters"]["from"];
                /** @description EXCLUSIVE upper bound on `created_at`. Malformed is a 400 - STRICT ISO 8601, either a full instant with an offset or a bare `YYYY-MM-DD`, which reads as UTC midnight. `"March 5 2026"` and `"2026"` are refused rather than guessed at. */
                until?: components["parameters"]["until"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of orders, each with its full `items` array. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OrderList"];
                };
            };
            400: components["responses"]["ValidationError"];
            401: components["responses"]["Unauthorized"];
        };
    };
    getStorefrontOrder: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The order. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Order"];
                };
            };
            404: components["responses"]["NotFound"];
        };
    };
    cancelStorefrontOrder: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The order, `status: "canceled"`. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Order"];
                };
            };
            404: components["responses"]["NotFound"];
            422: components["responses"]["StateRefusal"];
        };
    };
    getAdminSelf: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The admin key. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminKey"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    getAdminHealth: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The health report. Read `status`, not the HTTP code. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminHealth"];
                };
            };
            401: components["responses"]["Unauthorized"];
        };
    };
    listShops: {
        parameters: {
            query?: {
                /** @description 1-200. Out of range is a 400, never a clamp. */
                limit?: components["parameters"]["limit"];
                /** @description Opaque keyset cursor from a previous `next_cursor`. DO NOT PARSE OR CONSTRUCT ONE. Malformed is a 400, never a silent restart at page 1. */
                cursor?: components["parameters"]["cursor"];
                /** @description An unrecognised value is SILENTLY IGNORED and returns an unfiltered list. */
                status?: components["parameters"]["shopStatus"];
                /** @description Only the literal string `true` filters. Restricts to ACTIVE shops with a null `external_ref` - the same predicate as the admin-health count. */
                without_external_ref?: components["parameters"]["withoutExternalRef"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of shops. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ShopList"];
                };
            };
            400: components["responses"]["ValidationError"];
            403: components["responses"]["Forbidden"];
        };
    };
    createShop: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ShopCreate"];
            };
        };
        responses: {
            /** @description The shop, `status: "active"`. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Shop"];
                };
            };
            400: components["responses"]["ValidationError"];
            403: components["responses"]["Forbidden"];
        };
    };
    getShop: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The shop. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Shop"];
                };
            };
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    updateShop: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ShopUpdate"];
            };
        };
        responses: {
            /** @description The updated shop. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Shop"];
                };
            };
            400: components["responses"]["ValidationError"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    listShopKeys: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Every key this shop holds, newest first. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ShopKeyList"];
                };
            };
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    mintShopKey: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ShopKeyMint"];
            };
        };
        responses: {
            /** @description The key, including its plaintext exactly once. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MintedShopKey"];
                };
            };
            400: components["responses"]["ValidationError"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    rotateShopKey: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
                /** @description A shop key's `public_id`. */
                key_id: components["parameters"]["keyId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The new key, including its plaintext exactly once. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MintedShopKey"];
                };
            };
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    revokeShopKey: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
                /** @description A shop key's `public_id`. */
                key_id: components["parameters"]["keyId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The revoked key. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ShopKey"];
                };
            };
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    createProduct: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ProductCreate"];
            };
        };
        responses: {
            /** @description The product with its variants. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminProduct"];
                };
            };
            400: components["responses"]["ValidationError"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    createCoupon: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CouponCreate"];
            };
        };
        responses: {
            /** @description The coupon. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Coupon"];
                };
            };
            400: components["responses"]["ValidationError"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    createShippingMethod: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ShippingMethodCreate"];
            };
        };
        responses: {
            /** @description The shipping method. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ShippingMethod"];
                };
            };
            400: components["responses"]["ValidationError"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    listAdminKeys: {
        parameters: {
            query?: {
                /** @description Only the literal `empty` filters. Anything else returns every key. */
                allowlist?: "empty";
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Every admin key. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminKeyList"];
                };
            };
            403: components["responses"]["Forbidden"];
        };
    };
    mintAdminKey: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminKeyMint"];
            };
        };
        responses: {
            /** @description The admin key, including its plaintext exactly once. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MintedAdminKey"];
                };
            };
            400: components["responses"]["ValidationError"];
            403: components["responses"]["Forbidden"];
        };
    };
    revokeAdminKey: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The revoked key. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminKey"];
                };
            };
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            422: components["responses"]["StateRefusal"];
        };
    };
    listAdminProducts: {
        parameters: {
            query?: {
                /** @description 1-200. Out of range is a 400, never a clamp. */
                limit?: components["parameters"]["limit"];
                /** @description Opaque keyset cursor from a previous `next_cursor`. DO NOT PARSE OR CONSTRUCT ONE. Malformed is a 400, never a silent restart at page 1. */
                cursor?: components["parameters"]["cursor"];
                /** @description A shop's `public_id`. Unknown is a 404. Omitting it returns every shop's rows. */
                shop?: components["parameters"]["shopFilter"];
                /** @description An unrecognised value is SILENTLY IGNORED and returns an unfiltered list. */
                status?: components["parameters"]["productStatus"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of products. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminProductList"];
                };
            };
            400: components["responses"]["ValidationError"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    getAdminProduct: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The product. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminProduct"];
                };
            };
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    updateProduct: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ProductUpdate"];
            };
        };
        responses: {
            /** @description The updated product. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminProduct"];
                };
            };
            400: components["responses"]["ValidationError"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    publishProduct: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The product, `status: "published"`. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminProduct"];
                };
            };
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            422: components["responses"]["StateRefusal"];
        };
    };
    archiveProduct: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The product, `status: "archived"`. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminProduct"];
                };
            };
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            422: components["responses"]["StateRefusal"];
        };
    };
    createVariant: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["VariantCreate"];
            };
        };
        responses: {
            /** @description The new variant. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminVariant"];
                };
            };
            400: components["responses"]["ValidationError"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    getVariant: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The variant. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminVariant"];
                };
            };
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    updateVariant: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["VariantUpdate"];
            };
        };
        responses: {
            /** @description The updated variant. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminVariant"];
                };
            };
            400: components["responses"]["ValidationError"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    archiveVariant: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The variant, `status: "archived"`. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminVariant"];
                };
            };
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    getVariantInventory: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The stock report, tracked or untracked. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Inventory"];
                };
            };
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    setVariantInventory: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["InventorySet"];
            };
        };
        responses: {
            /** @description The stock report. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Inventory"];
                };
            };
            400: components["responses"]["ValidationError"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            422: components["responses"]["StateRefusal"];
        };
    };
    listAdminOrders: {
        parameters: {
            query?: {
                /** @description 1-200. Out of range is a 400, never a clamp. */
                limit?: components["parameters"]["limit"];
                /** @description Opaque keyset cursor from a previous `next_cursor`. DO NOT PARSE OR CONSTRUCT ONE. Malformed is a 400, never a silent restart at page 1. */
                cursor?: components["parameters"]["cursor"];
                /** @description A shop's `public_id`. Unknown is a 404. Omitting it returns every shop's rows. */
                shop?: components["parameters"]["shopFilter"];
                /** @description An unknown value IS a 400 on the order listings. */
                status?: components["parameters"]["orderStatus"];
                /** @description INCLUSIVE lower bound on `created_at`. Malformed is a 400 - STRICT ISO 8601, either a full instant with an offset or a bare `YYYY-MM-DD`, which reads as UTC midnight. `"March 5 2026"` and `"2026"` are refused rather than guessed at. */
                from?: components["parameters"]["from"];
                /** @description EXCLUSIVE upper bound on `created_at`. Malformed is a 400 - STRICT ISO 8601, either a full instant with an offset or a bare `YYYY-MM-DD`, which reads as UTC midnight. `"March 5 2026"` and `"2026"` are refused rather than guessed at. */
                until?: components["parameters"]["until"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of orders. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OrderList"];
                };
            };
            400: components["responses"]["ValidationError"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    getAdminOrder: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The order. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Order"];
                };
            };
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    fulfillOrder: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The order, `status: "fulfilled"`. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Order"];
                };
            };
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            422: components["responses"]["StateRefusal"];
        };
    };
    cancelAdminOrder: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The order, `status: "canceled"`. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Order"];
                };
            };
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            422: components["responses"]["StateRefusal"];
        };
    };
    listCoupons: {
        parameters: {
            query?: {
                /** @description 1-200. Out of range is a 400, never a clamp. */
                limit?: components["parameters"]["limit"];
                /** @description Opaque keyset cursor from a previous `next_cursor`. DO NOT PARSE OR CONSTRUCT ONE. Malformed is a 400, never a silent restart at page 1. */
                cursor?: components["parameters"]["cursor"];
                /** @description A shop's `public_id`. Unknown is a 404. Omitting it returns every shop's rows. */
                shop?: components["parameters"]["shopFilter"];
                /** @description Only the literal strings `true` and `false` filter. Anything else is ignored. */
                active?: components["parameters"]["activeFilter"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of coupons. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CouponList"];
                };
            };
            400: components["responses"]["ValidationError"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    getCoupon: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The coupon. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Coupon"];
                };
            };
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    updateCoupon: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CouponUpdate"];
            };
        };
        responses: {
            /** @description The updated coupon. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Coupon"];
                };
            };
            400: components["responses"]["ValidationError"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    listShippingMethods: {
        parameters: {
            query?: {
                /** @description 1-200. Out of range is a 400, never a clamp. */
                limit?: components["parameters"]["limit"];
                /** @description Opaque keyset cursor from a previous `next_cursor`. DO NOT PARSE OR CONSTRUCT ONE. Malformed is a 400, never a silent restart at page 1. */
                cursor?: components["parameters"]["cursor"];
                /** @description A shop's `public_id`. Unknown is a 404. Omitting it returns every shop's rows. */
                shop?: components["parameters"]["shopFilter"];
                /** @description Only the literal strings `true` and `false` filter. Anything else is ignored. */
                active?: components["parameters"]["activeFilter"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of shipping methods. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ShippingMethodList"];
                };
            };
            400: components["responses"]["ValidationError"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    getShippingMethod: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The shipping method. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ShippingMethod"];
                };
            };
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    updateShippingMethod: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ShippingMethodUpdate"];
            };
        };
        responses: {
            /** @description The updated shipping method. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ShippingMethod"];
                };
            };
            400: components["responses"]["ValidationError"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    listEventTypes: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The six types. `subscribed_events: null` on an endpoint means all of them. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["EventTypeList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listWebhookEndpoints: {
        parameters: {
            query?: {
                /** @description 1-200. Out of range is a 400, never a clamp. */
                limit?: components["parameters"]["limit"];
                /** @description Opaque keyset cursor from a previous `next_cursor`. DO NOT PARSE OR CONSTRUCT ONE. Malformed is a 400, never a silent restart at page 1. */
                cursor?: components["parameters"]["cursor"];
                /** @description A shop's `public_id`. Unknown is a 404. Omitting it returns every shop's rows. */
                shop?: components["parameters"]["shopFilter"];
                /** @description A TRI-STATE: absent, `true` or `false`. Anything else is a 400, never a silent "absent" - unlike the tolerant shop and product filters. `false` uses the same predicate as the `disabled_endpoints` health count. */
                enabled?: components["parameters"]["enabledFilter"];
                /** @description One of the six catalogue types. An unknown value IS a 400. */
                event_type?: components["parameters"]["eventTypeFilter"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of endpoints. The secret is never here - only `secret_last4`. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["WebhookEndpointList"];
                };
            };
            400: components["responses"]["ValidationError"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    listShopWebhookEndpoints: {
        parameters: {
            query?: {
                /** @description 1-200. Out of range is a 400, never a clamp. */
                limit?: components["parameters"]["limit"];
                /** @description Opaque keyset cursor from a previous `next_cursor`. DO NOT PARSE OR CONSTRUCT ONE. Malformed is a 400, never a silent restart at page 1. */
                cursor?: components["parameters"]["cursor"];
                /** @description A TRI-STATE: absent, `true` or `false`. Anything else is a 400, never a silent "absent" - unlike the tolerant shop and product filters. `false` uses the same predicate as the `disabled_endpoints` health count. */
                enabled?: components["parameters"]["enabledFilter"];
            };
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of this shop's endpoints. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["WebhookEndpointList"];
                };
            };
            400: components["responses"]["ValidationError"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    createWebhookEndpoint: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["WebhookEndpointCreate"];
            };
        };
        responses: {
            /** @description The endpoint, WITH its plaintext `secret`. Shown exactly once, here, and by no read route ever. Capture it now; a lost secret is rotated, not recovered. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MintedWebhookEndpoint"];
                };
            };
            400: components["responses"]["ValidationError"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["EndpointLimit"];
        };
    };
    getWebhookEndpoint: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A webhook endpoint's `public_id`. NOTE there is no shop segment on these routes - the shop is read off the endpoint itself, because cross-shop reach is what a `wad_` key is for. */
                id: components["parameters"]["endpointId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The endpoint. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["WebhookEndpoint"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    deleteWebhookEndpoint: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A webhook endpoint's `public_id`. NOTE there is no shop segment on these routes - the shop is read off the endpoint itself, because cross-shop reach is what a `wad_` key is for. */
                id: components["parameters"]["endpointId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @constant */
                        deleted: true;
                        /** Format: uuid */
                        public_id: string;
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    updateWebhookEndpoint: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A webhook endpoint's `public_id`. NOTE there is no shop segment on these routes - the shop is read off the endpoint itself, because cross-shop reach is what a `wad_` key is for. */
                id: components["parameters"]["endpointId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["WebhookEndpointUpdate"];
            };
        };
        responses: {
            /** @description The updated endpoint. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["WebhookEndpoint"];
                };
            };
            400: components["responses"]["ValidationError"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    rotateWebhookEndpointSecret: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A webhook endpoint's `public_id`. NOTE there is no shop segment on these routes - the shop is read off the endpoint itself, because cross-shop reach is what a `wad_` key is for. */
                id: components["parameters"]["endpointId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The endpoint with its NEW plaintext `secret`. Shown exactly once. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MintedWebhookEndpoint"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    enableWebhookEndpoint: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A webhook endpoint's `public_id`. NOTE there is no shop segment on these routes - the shop is read off the endpoint itself, because cross-shop reach is what a `wad_` key is for. */
                id: components["parameters"]["endpointId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The endpoint, `enabled: true`. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["WebhookEndpoint"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    disableWebhookEndpoint: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A webhook endpoint's `public_id`. NOTE there is no shop segment on these routes - the shop is read off the endpoint itself, because cross-shop reach is what a `wad_` key is for. */
                id: components["parameters"]["endpointId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The endpoint, `enabled: false`, with a `disabled_reason`. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["WebhookEndpoint"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    testWebhookEndpoint: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A webhook endpoint's `public_id`. NOTE there is no shop segment on these routes - the shop is read off the endpoint itself, because cross-shop reach is what a `wad_` key is for. */
                id: components["parameters"]["endpointId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The ping's outcome. A 200 here means the CALL was made, not that it succeeded. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PingResult"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    listWebhookEvents: {
        parameters: {
            query?: {
                /** @description 1-200. Out of range is a 400, never a clamp. */
                limit?: components["parameters"]["limit"];
                /** @description Opaque keyset cursor from a previous `next_cursor`. DO NOT PARSE OR CONSTRUCT ONE. Malformed is a 400, never a silent restart at page 1. */
                cursor?: components["parameters"]["cursor"];
                /** @description A shop's `public_id`. Unknown is a 404. Omitting it returns every shop's rows. */
                shop?: components["parameters"]["shopFilter"];
                /** @description One of the six catalogue types. An unknown value IS a 400. */
                event_type?: components["parameters"]["eventTypeFilter"];
                /** @description INCLUSIVE lower bound on `created_at`. Malformed is a 400 - STRICT ISO 8601, either a full instant with an offset or a bare `YYYY-MM-DD`, which reads as UTC midnight. `"March 5 2026"` and `"2026"` are refused rather than guessed at. */
                from?: components["parameters"]["from"];
                /** @description EXCLUSIVE upper bound on `created_at`. Malformed is a 400 - STRICT ISO 8601, either a full instant with an offset or a bare `YYYY-MM-DD`, which reads as UTC midnight. `"March 5 2026"` and `"2026"` are refused rather than guessed at. */
                until?: components["parameters"]["until"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of events. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["WebhookEventList"];
                };
            };
            400: components["responses"]["ValidationError"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    listWebhookDeliveries: {
        parameters: {
            query?: {
                /** @description 1-200. Out of range is a 400, never a clamp. */
                limit?: components["parameters"]["limit"];
                /** @description Opaque keyset cursor from a previous `next_cursor`. DO NOT PARSE OR CONSTRUCT ONE. Malformed is a 400, never a silent restart at page 1. */
                cursor?: components["parameters"]["cursor"];
                /** @description A shop's `public_id`. Unknown is a 404. Omitting it returns every shop's rows. */
                shop?: components["parameters"]["shopFilter"];
                /** @description A webhook endpoint's `public_id`. Unknown is a 404. */
                endpoint?: components["parameters"]["endpointFilter"];
                /** @description One of the six catalogue types. An unknown value IS a 400. */
                event_type?: components["parameters"]["eventTypeFilter"];
                /** @description ONLY THE LITERAL `true` FILTERS - anything else returns the unfiltered list. It selects deliveries whose event type this service declares as crossing a service boundary, which is a declaration by THIS service about where an event is meant to go and never evidence about the receiver. */
                cross_service?: "true";
                /** @description An unknown value IS a 400. `pending` and `dead_lettered` use the same predicates as the two delivery health counts. */
                status?: components["parameters"]["deliveryStatusFilter"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of deliveries. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["WebhookDeliveryList"];
                };
            };
            400: components["responses"]["ValidationError"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    redeliverWebhookDelivery: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A webhook delivery ROW's `public_id`. Not `X-Delivery-Id`, which changes per attempt. */
                id: components["parameters"]["deliveryId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Queued and bursting. Not yet delivered. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @constant */
                        redelivering: true;
                        /** Format: uuid */
                        public_id: string;
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    listJobs: {
        parameters: {
            query?: {
                /** @description 1-200. Out of range is a 400, never a clamp. */
                limit?: components["parameters"]["limit"];
                /** @description Opaque keyset cursor from a previous `next_cursor`. DO NOT PARSE OR CONSTRUCT ONE. Malformed is a 400, never a silent restart at page 1. */
                cursor?: components["parameters"]["cursor"];
                /** @description One of the seven job types. An unknown value IS a 400. */
                type?: components["parameters"]["jobTypeFilter"];
                /** @description An unknown value IS a 400. NOTE `due` is NOT a column value - it is the drain's own eligibility predicate (pending, not canceled, `run_after` in the past), and it is the filter that matches the `due_jobs` health count. `dead` also uses the health predicate, which EXCLUDES canceled rows; a plain column equality would have included them. */
                status?: components["parameters"]["jobStatusFilter"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of jobs. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["JobList"];
                };
            };
            400: components["responses"]["ValidationError"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    retryJob: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A job's `public_id`. */
                id: components["parameters"]["jobId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The requeued job. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Job"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    cancelJob: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A job's `public_id`. */
                id: components["parameters"]["jobId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The canceled job. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Job"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    listInboundEvents: {
        parameters: {
            query?: {
                /** @description 1-200. Out of range is a 400, never a clamp. */
                limit?: components["parameters"]["limit"];
                /** @description Opaque keyset cursor from a previous `next_cursor`. DO NOT PARSE OR CONSTRUCT ONE. Malformed is a 400, never a silent restart at page 1. */
                cursor?: components["parameters"]["cursor"];
                /** @description Only `unknown_account` filters, and it is the `unroutable_inbound_events` drill-down. ANYTHING ELSE IS IGNORED rather than refused, so a typo returns the unfiltered list. */
                reason_code?: components["parameters"]["reasonCodeFilter"];
                /** @description Only `refused` filters, and it is the `refused_inbound_events` drill-down. Anything else is IGNORED rather than refused. */
                outcome?: components["parameters"]["outcomeFilter"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of ledger rows. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["InboundEventList"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    listAuditLog: {
        parameters: {
            query?: {
                /** @description 1-200. Out of range is a 400, never a clamp. */
                limit?: components["parameters"]["limit"];
                /** @description Opaque keyset cursor from a previous `next_cursor`. DO NOT PARSE OR CONSTRUCT ONE. Malformed is a 400, never a silent restart at page 1. */
                cursor?: components["parameters"]["cursor"];
                /** @description A CLOSED SET, and an unknown value is a 400 rather than an empty page - HR-19's last MUST, because an empty page reads as "nothing happened". */
                actor_type?: components["parameters"]["actorTypeFilter"];
                /** @description Exact match on the action name, e.g. `order.checkout`. NOT a prefix and not a pattern; the action vocabulary grows per phase and is listed in `operations.md`. */
                action?: components["parameters"]["auditActionFilter"];
                /** @description The kind of resource acted on, e.g. `order`, `shop`, `product`, `webhook_endpoint`. */
                target_type?: components["parameters"]["targetTypeFilter"];
                /** @description REQUIRES `target_type` - an id alone does not identify a target, and the unpaired query would scan without an index and return another kind's history. Alone it is a 400. */
                target_id?: components["parameters"]["targetIdFilter"];
                /** @description INCLUSIVE lower bound on `created_at`. Malformed is a 400 - STRICT ISO 8601, either a full instant with an offset or a bare `YYYY-MM-DD`, which reads as UTC midnight. `"March 5 2026"` and `"2026"` are refused rather than guessed at. */
                from?: components["parameters"]["from"];
                /** @description EXCLUSIVE upper bound on `created_at`. Malformed is a 400 - STRICT ISO 8601, either a full instant with an offset or a bare `YYYY-MM-DD`, which reads as UTC midnight. `"March 5 2026"` and `"2026"` are refused rather than guessed at. */
                until?: components["parameters"]["until"];
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of trail rows. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuditRowList"];
                };
            };
            400: components["responses"]["ValidationError"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    getPaymentCredential: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The credential metadata. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PaymentCredential"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            /** @description 404 "This shop has no payment credential." Also how you confirm one specific shop is unprovisioned, rather than listing them all. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    putPaymentCredential: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description A UUIDv7. A segment that cannot be a UUID answers 404, never 400 or 500. */
                public_id: components["parameters"]["publicId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["PaymentCredentialRequest"];
            };
        };
        responses: {
            /** @description The readback - `last4` and a fingerprint. NEVER the secret. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PaymentCredential"];
                };
            };
            400: components["responses"]["ValidationError"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    receivePaymentServiceEvent: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** @description The eleven-member event envelope. Parsed LENIENTLY - unknown members are ignored rather than refused, because here an unknown key is the emitter adding a member and refusing it would turn their additive change into our outage. That is the opposite of every other request body in this spec. */
        requestBody: {
            content: {
                "application/json": components["schemas"]["InboundEnvelope"];
            };
        };
        responses: {
            /** @description Received. `outcome` carries the decision: `accepted` (recorded and queued), `ignored` (recorded, no action - with a `reason_code`) or `refused` (recorded, hop limit reached). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["InboundReceipt"];
                };
            };
            400: components["responses"]["InboundRejected"];
            500: components["responses"]["InternalError"];
        };
    };
    cronWebhooksDrain: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description What the tick claimed, ran and failed. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DrainTickResult"];
                };
            };
            401: components["responses"]["Unauthorized"];
        };
    };
    cronWebhooksDrainByHand: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description What the tick claimed, ran and failed. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DrainTickResult"];
                };
            };
            401: components["responses"]["Unauthorized"];
        };
    };
    cronSweeps: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description What the tick enqueued, and what the drain that followed it did. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SweepsTickResult"];
                };
            };
            401: components["responses"]["Unauthorized"];
        };
    };
    cronSweepsByHand: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description What the tick enqueued, and what the drain that followed it did. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SweepsTickResult"];
                };
            };
            401: components["responses"]["Unauthorized"];
        };
    };
}
